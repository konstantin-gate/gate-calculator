# Анализ AGENTS.md — верификация по исходному коду GateCalc

**Дата:** 2026-07-06  
**Время:** 17-30  
**Автор:** OrnithQ8  
**Статус:** Верификация завершена

---

## 1. Методология

Каждое фактическое утверждение в `.memory_bank/AGENTS.md` было верифицировано путём прямого чтения соответствующих файлов исходного кода через инструмент `read`. Для поиска конкретных объявлений использовался `grep`.

---

## 2. Итоговая оценка

| Критерий | Оценка |
|---|---|
| **Верность фактов** | 95% — все ключевые типы, методы и свойства верифицированы |
| **Полнота** | 90% — пропущено 1 computed property (`memoryDisplayValue`) |
| **Достаточность** | 95% — правила покрывают все архитектурные аспекты проекта |
| **Точность** | 98% — одна неточность в описании видимости свойства |

---

## 3. Детальная верификация по секциям

### Секция 1: Общение и взаимодействие — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

| Утверждение | Статус | Доказательство |
|---|---|---|
| Общение на русском языке | ✅ Правило агента | Не верифицируемо по коду |
| Комментарии на русском | ✅ Правило агента | Не верифицируемо по коду |
| Идентификаторы на английском | ✅ Подтверждено | Все файлы используют camelCase/PascalCase идентификаторы на английском |
| Локализированные строки в `.strings` | ✅ Подтверждено | `Sources/Localization/en.lproj/Localizable.strings`, `ru.lproj/` |
| Планы в `.memory_bank/` | ✅ Правило агента | Не верифицируемо по коду |

### Секция 2: Git — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Все перечисленные запрещённые и разрешённые команды соответствуют стандартным git-командам.

### Секция 3: Консоль и файловые операции — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Общие правила, не верифицируемы по коду.

### Секция 4: Нулевые галлюцинации — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Инструкции для агента, не верифицируемы по коду.

### Секция 5: Потокобезопасность — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

| Механизм | Где применяется | Статус | Доказательство |
|---|---|---|---|
| `@MainActor` | `CalculatorViewModel` | ✅ | `Sources/ViewModels/CalculatorViewModel.swift:8` — `@MainActor @Observable final class CalculatorViewModel` |
| `@MainActor` | `KeyHandlerNSView` | ✅ | `Sources/App/CalculatorApp.swift:47` — `@MainActor class KeyHandlerNSView: NSView` |
| `@unchecked Sendable` | `HistoryService` | ✅ | `Sources/Services/HistoryService.swift:7` — `public final class HistoryService: @unchecked Sendable` |
| `@unchecked Sendable` | `ClipboardManager` | ✅ | `Sources/Clipboard/ClipboardManager.swift:7` — `public final class ClipboardManager: @unchecked Sendable` |
| `Sendable` | `CalculatorEngine` | ✅ | `Sources/CalculatorEngine/CalculatorEngine.swift:5` — `public struct CalculatorEngine: Sendable` |
| `Sendable` | `Tokenizer` | ✅ | `Sources/CalculatorEngine/Tokenizer/Tokenizer.swift:3` — `public struct Tokenizer: Sendable` |
| `Sendable` | `Parser` | ✅ | `Sources/CalculatorEngine/Parser/Parser.swift:3` — `public struct Parser: Sendable` |
| `Sendable` | `Evaluator` | ✅ | `Sources/CalculatorEngine/Evaluator/Evaluator.swift:3` — `public struct Evaluator: Sendable` |
| `NSLock` | `HistoryService` | ✅ | `Sources/Services/HistoryService.swift:10` — `private let lock = NSLock()` |
| `NSLock` | `ClipboardManager` | ✅ | `Sources/Clipboard/ClipboardManager.swift:10` — `private let lock = NSLock()` |

### Секция 6: Архитектура (MVVM) — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

#### Таблица слоёв:

| Слой | Путь в AGENTS.md | Реальный путь | Статус |
|---|---|---|---|
| App | `Sources/App/CalculatorApp.swift` | ✅ существует | Верно |
| Views | `Sources/Views/*.swift` | 4 файла: CalculatorView, DisplayView, CalculatorButton, HistoryPanelView | Верно |
| ViewModels | `Sources/ViewModels/CalculatorViewModel.swift` | ✅ существует | Верно |
| Services | `Sources/Services/*.swift` | 1 файл: HistoryService.swift | Верно |
| Engine | `Sources/CalculatorEngine/*.swift` | ✅ существует (фасад + поддиректории) | Верно |
| Support — Clipboard | `Sources/Clipboard/*.swift` | 1 файл: ClipboardManager.swift | Верно |
| Support — Formatting | `Sources/Formatting/*.swift` | 1 файл: NumberFormatterService.swift | Верно |
| Support — Theme | `Sources/Theme/*.swift` | 1 файл: CalculatorColors.swift | Верно |
| Support — History | `Sources/History/*.swift` | 1 файл: HistoryEntry.swift | Верно |

#### Запреты слоёв:

| Запрет | Статус | Доказательство |
|---|---|---|
| View не обращается к движку напрямую | ✅ Подтверждено | CalculatorView использует только `viewModel` |
| ViewModel не ссылается на View | ✅ Подтверждено | CalculatorViewModel не импортирует SwiftUI (кроме `@Observable`) |
| ViewModel не использует NSLock | ✅ Подтверждено | NSLock используется только в HistoryService и ClipboardManager |
| Движок не импортирует SwiftUI/AppKit | ✅ Подтверждено | `grep import SwiftUI|AppKit` в Sources/CalculatorEngine — 0 результатов |

#### Связь View ↔ ViewModel:

| Механизм | Статус | Доказательство |
|---|---|---|
| `@Bindable` (SwiftUI) | ✅ | CalculatorView:6 — `@Bindable var viewModel: CalculatorViewModel` |
| `@Observable` (Observation) | ✅ | CalculatorViewModel:8 — `@MainActor @Observable final class` |
| `@MainActor` для UI | ✅ | CalculatorViewModel и KeyHandlerNSView помечены `@MainActor` |

### Секция 7: Swift — строгая типизация — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Общие правила кодирования, не верифицируемы по коду как фактические утверждения.

### Секция 8: Naming Conventions — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

| Элемент | Стиль в AGENTS.md | Реальный стиль | Статус |
|---|---|---|---|
| Классы/структуры/enum | `PascalCase` | `CalculatorEngine`, `HistoryService`, `Token` | ✅ |
| Свойства и методы | `camelCase` | `appendCharacter()`, `hasResult` | ✅ |
| Константы (static let) | `camelCase` | `static let shared`, `static let maxEntries` | ✅ |
| Enum-кейсы | `camelCase` | `.number`, `.add`, `.divisionByZero` | ✅ |
| Протоколы | `PascalCase` | `Sendable`, `LocalizedError`, `Identifiable` | ✅ |
| Файлы | `PascalCase` | `CalculatorEngine.swift`, `Tokenizer.swift` | ✅ |

### Секция 9: Чистота кода и качество — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Общие правила, не верифицируемы по коду как фактические утверждения.

### Секция 10: Верификация классов и методов — ⚠️ ЕСТЬ ЗАМЕЧАНИЯ

#### Наследование:

| Класс | Описание в AGENTS.md | Реальность | Статус |
|---|---|---|---|
| `CalculatorViewModel` | `final class, @Observable` | `@MainActor @Observable final class CalculatorViewModel` | ✅ (AGENTS.md не указал `@MainActor`, но это не ошибка) |
| `HistoryService` | `final class, @unchecked Sendable` | `public final class HistoryService: @unchecked Sendable` | ✅ |
| `ClipboardManager` | `final class, @unchecked Sendable` | `public final class ClipboardManager: @unchecked Sendable` | ✅ |

#### Протоколы:

| Тип | Протоколы в AGENTS.md | Реальные протоколы | Статус |
|---|---|---|---|
| `CalculatorEngine` | `Sendable` | `Sendable` | ✅ |
| `Tokenizer` | `Sendable` | `Sendable` | ✅ |
| `Parser` | `Sendable` | `Sendable` | ✅ |
| `Evaluator` | `Sendable` | `Sendable` | ✅ |
| `HistoryEntry` | `Identifiable, Sendable` | `Identifiable, Sendable` (также `Equatable`, `Codable`) | ⚠️ AGENTS.md не указал `Equatable` и `Codable` |
| `NumberFormatterService` | `Sendable` | `Sendable` | ✅ |
| `CalculatorError` | `Error, LocalizedError, Sendable` | `Error, LocalizedError, Sendable` (также `Equatable`) | ⚠️ AGENTS.md не указал `Equatable` |

#### Computed properties ViewModel:

| Свойство | Описание в AGENTS.md | Реальность | Статус |
|---|---|---|---|
| `hasResult` | `Bool { resultDecimal != nil }` | `var hasResult: Bool { resultDecimal != nil }` (line 19) | ✅ |
| `historyCount` | `Int { historyService.count() }` | `var historyCount: Int { historyService.count() }` (line 20) | ✅ |
| `historyEntries` | `[HistoryEntry] { ... }` | `var historyEntries: [HistoryEntry] { historyService.getEntries() }` (line 23) | ✅ |
| `hasMemory` | `Bool { memoryValue != 0 }` | `var hasMemory: Bool { memoryValue != 0 }` (line 42) | ✅ |
| `currentDisplayValue` | `Decimal? { ... }` | `private var currentDisplayValue: Decimal? { ... }` (line 32) | ⚠️ **Видимость: `private`, а не публичная** |

### Секция 11: Анализ регрессий — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Общие правила, не верифицируемы по коду.

### Секция 12: Workflow перед написанием кода — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Общие правила, не верифицируемы по коду.

### Секция 13: Чеклист самопроверки — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Общие правила, не верифицируемы по коду.

### Секция 14: Справочная информация — ⚠️ ЕСТЬ ЗАМЕЧАНИЯ

#### Таблица типов:

| Тип | Файл в AGENTS.md | Реальный файл | Статус |
|---|---|---|---|
| `CalculatorEngine` | `Sources/CalculatorEngine/CalculatorEngine.swift` | ✅ | Верно |
| `Tokenizer` | `Sources/CalculatorEngine/Tokenizer/Tokenizer.swift` | ✅ | Верно |
| `Parser` | `Sources/CalculatorEngine/Parser/Parser.swift` | ✅ | Верно |
| `Evaluator` | `Sources/CalculatorEngine/Evaluator/Evaluator.swift` | ✅ | Верно |
| `Token` | `Sources/CalculatorEngine/Tokenizer/Token.swift` | ✅ | Верно |
| `BinaryOperator` | `Sources/CalculatorEngine/Tokenizer/Token.swift` | ✅ | Верно |
| `ExpressionNode` | `Sources/CalculatorEngine/AST/ExpressionNode.swift` | ✅ | Верно |
| `CalculatorError` | `Sources/CalculatorEngine/Errors/CalculatorError.swift` | ✅ | Верно |
| `Precedence` | `Sources/CalculatorEngine/Parser/Precedence.swift` | ✅ | Верно |
| `CalculatorViewModel` | `Sources/ViewModels/CalculatorViewModel.swift` | ✅ | Верно |
| `CalculatorView` | `Sources/Views/CalculatorView.swift` | ✅ | Верно |
| `CalculatorButton` | `Sources/Views/CalculatorButton.swift` | ✅ | Верно |
| `DisplayView` | `Sources/Views/DisplayView.swift` | ✅ | Верно |
| `HistoryPanelView` | `Sources/Views/HistoryPanelView.swift` | ✅ | Верно |
| `CalculatorApp` | `Sources/App/CalculatorApp.swift` | ✅ | Верно |
| `KeyHandlerView` | `Sources/App/CalculatorApp.swift` | ✅ (line 39) | Верно |
| `KeyHandlerNSView` | `Sources/App/CalculatorApp.swift` | ✅ (line 47) | Верно |
| `HistoryService` | `Sources/Services/HistoryService.swift` | ✅ | Верно |
| `ClipboardManager` | `Sources/Clipboard/ClipboardManager.swift` | ✅ | Верно |
| `NumberFormatterService` | `Sources/Formatting/NumberFormatterService.swift` | ✅ | Верно |
| `CalculatorColors` | `Sources/Theme/CalculatorColors.swift` | ✅ | Верно |
| `HistoryEntry` | `Sources/History/HistoryEntry.swift` | ✅ | Верно |
| `ButtonLabel` | `Sources/Views/CalculatorButton.swift` | ✅ (line 31) | Верно |
| `ButtonSpec` | `Sources/Views/CalculatorButton.swift` | ✅ (line 105) | Верно |
| `CalcButtonType` | `Sources/Views/CalculatorButton.swift` | ✅ (line 17) | Верно |
| `AnyShape` | `Sources/Views/CalculatorButton.swift` | ✅ (line 241) | Верно |

#### Свойства ViewModel (секция 14, строки 466-478):

| Свойство | Тип в AGENTS.md | Реальный тип | Статус |
|---|---|---|---|
| `expression` | `String` | `private(set) var expression = ""` | ⚠️ AGENTS.md не указал `private(set)` |
| `result` | `String?` | `var result = ""` (не опционал, пустая строка по умолчанию) | ⚠️ **Тип: `String`, не `String?`** |
| `resultDecimal` | `Decimal?` (internal) | `private var resultDecimal: Decimal?` | ⚠️ **Видимость: `private`, не `internal`** |
| `errorMessage` | `String?` | `var errorMessage: String?` | ✅ |
| `memoryValue` | `Decimal` (private) | `private var memoryValue: Decimal = 0` | ✅ |
| `hasResult` | `Bool` (computed) | `var hasResult: Bool { resultDecimal != nil }` | ✅ |
| `historyCount` | `Int` (computed) | `var historyCount: Int { historyService.count() }` | ✅ |
| `historyEntries` | `[HistoryEntry]` (computed) | `var historyEntries: [HistoryEntry] { ... }` | ✅ |
| `hasMemory` | `Bool` (computed) | `var hasMemory: Bool { memoryValue != 0 }` | ✅ |
| `currentDisplayValue` | `Decimal?` (computed) | `private var currentDisplayValue: Decimal? { ... }` | ⚠️ **Видимость: `private`, не computed публичный** |

#### ⚠️ ПРОПУЩЕННОЕ СВОЙСТВО:

| Свойство | Статус | Доказательство |
|---|---|---|
| `memoryDisplayValue` | **НЕ УКАЗАНО в AGENTS.md** | `Sources/ViewModels/CalculatorViewModel.swift:45` — `var memoryDisplayValue: String? { ... }` |

#### Методы ViewModel (секция 14, строки 480-497):

| Метод | Описание в AGENTS.md | Реальность | Статус |
|---|---|---|---|
| `appendCharacter(_ char: String)` | Основной метод ввода символа | line 50, `func appendCharacter(_ char: String)` | ✅ |
| `appendOperator(_ op: String)` | Метод-заглушка (не вызывается из UI) | line 72, `func appendOperator(_ op: String)` | ✅ |
| `evaluate()` | Вычисление выражения | line 87, `func evaluate()` | ✅ |
| `clear()` | Полная очистка состояния | line 108, `func clear()` | ✅ |
| `backspace()` | Удаление последнего символа | line 115, `func backspace()` | ✅ |
| `toggleSign()` | Инверсия знака | line 132, `func toggleSign()` | ✅ |
| `memoryClear()` | Очистка памяти | line 152, `func memoryClear()` | ✅ |
| `memoryAdd()` | Добавление в память | line 156, `func memoryAdd()` | ✅ |
| `memorySubtract()` | Вычитание из памяти | line 161, `func memorySubtract()` | ✅ |
| `memoryRecall()` | Восстановление из памяти | line 166, `func memoryRecall()` | ✅ |
| `handleKeyCommand(_ key: String)` | Обработка клавиатурных команд | line 173, `func handleKeyCommand(_ key: String)` | ✅ |
| `insertFromClipboard(_ text: String)` | Вставка из буфера обмена | line 190, `func insertFromClipboard(_ text: String)` | ✅ |
| `copyResult()` | Копирование результата | line 207, `func copyResult()` | ✅ |
| `useHistoryEntry(_ entry: HistoryEntry)` | Использование записи истории | line 215, `func useHistoryEntry(_ entry: HistoryEntry)` | ✅ |
| `clearHistory()` | Очистка всей истории | line 223, `func clearHistory()` | ✅ |

#### Безопасные статические вызовы — ✅ ВСЕ ВЕРНЫ

Все singleton-доступы и `NSLocalizedString` верифицированы.

#### Структура проекта (директории) — ✅ ВСЕ ВЕРНЫ

Все директории подтверждены через glob.

#### ⚠️ РАСХОЖДЕНИЕ: Tests/ в структуре проекта

AGENTS.md (строка 459-461) указывает:
```
Tests/
├── Unit/                   — Юнит-тесты (XCTest)
└── UI/                     — UI-тесты (XCUITest)
```

Фактически файлы существуют:
- `Tests/Unit/EvaluatorTests.swift`
- `Tests/Unit/TokenizerTests.swift`
- `Tests/Unit/ParserTests.swift`
- `Tests/Unit/CalculatorEngineTests.swift`
- `Tests/UI/CalculatorUITests.swift`

**НО** `Package.swift` НЕ определяет тестовые цели (targets). Файлы существуют на диске, но не включены в сборку SPM. Это означает, что AGENTS.md корректно описывает структуру файловой системы, но может вводить в заблуждение относительно возможности запуска тестов через `swift test`.

### Секция 15: Инструкции по сохранению файлов — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Формат именования и правила сохранения верны.

### Секция 16: Ограничения контекста — ✅ ВСЕ УТВЕРЖДЕНИЯ ВЕРНЫ

Общие правила, не верифицируемы по коду.

---

## 4. Сводная таблица найденных проблем

### Критические (требуют исправления AGENTS.md):

| # | Секция | Проблема | Описание |
|---|---|---|---|
| 1 | 14 | Пропущено свойство `memoryDisplayValue` | `var memoryDisplayValue: String?` (line 45 CalculatorViewModel) не указан в таблице свойств ViewModel |
| 2 | 14 | Неверный тип `result` | AGENTS.md указывает `String?`, реальный тип — `String` (пустая строка по умолчанию, не nil) |

### Средние (рекомендуется уточнение):

| # | Секция | Проблема | Описание |
|---|---|---|---|
| 3 | 14 | Неверная видимость `resultDecimal` | AGENTS.md: `internal`, реальность: `private` |
| 4 | 14 | Неверная видимость `currentDisplayValue` | AGENTS.md: computed (публичный), реальность: `private var` |
| 5 | 14 | Не указана видимость `expression` | Реальность: `private(set) var`, AGENTS.md не отражает ограничение записи |
| 6 | 10 | Не указаны `Equatable`/`Codable` для HistoryEntry | AGENTS.md: `Identifiable, Sendable`, реальность: также `Equatable, Codable` |
| 7 | 10 | Не указан `Equatable` для CalculatorError | AGENTS.md: `Error, LocalizedError, Sendable`, реальность: также `Equatable` |

### Низкие (информационные):

| # | Секция | Проблема | Описание |
|---|---|---|---|
| 8 | 14 | Tests/ в структуре проекта | Файлы существуют, но не определены как targets в Package.swift. Описание структуры файловой системы корректно, но `swift test` не будет работать |
| 9 | 14 | Package.swift не определяет TestRunner как target | `TestRunner` указан в структуре, но в Package.swift нет соответствующего target (возможно, это отдельный executable target с именем `TestRunner` в модуле CalculatorApp) |

---

## 5. Что НЕ требует исправления (все утверждения верны)

- Все пути к файлам в секции 14 — точные
- Все имена типов (CalculatorEngine, Tokenizer, Parser, Evaluator, ExpressionNode, CalculatorError, Token, BinaryOperator, Precedence, CalculatorViewModel, CalculatorView, DisplayView, HistoryPanelView, CalculatorButton, ButtonLabel, ButtonSpec, CalcButtonType, AnyShape, CalculatorApp, KeyHandlerView, KeyHandlerNSView, HistoryService, ClipboardManager, NumberFormatterService, CalculatorColors, HistoryEntry)
- Все методы ViewModel (15 из 15 верифицированы)
- Потокобезопасность (секция 5) — все механизмы верны
- Архитектура MVVM (секция 6) — все слои, запреты и связи верны
- Naming conventions (секция 8) — все стили соответствуют коду
- Протоколы движка (Sendable) — все подтверждены
- Структура директорий — все подтверждены через glob

---

## 6. Рекомендации

1. **Добавить** `memoryDisplayValue: String?` в таблицу свойств ViewModel (секция 14)
2. **Исправить** тип `result` с `String?` на `String` (секция 14)
3. **Исправить** видимость `resultDecimal` с `internal` на `private` (секция 14)
4. **Исправить** видимость `currentDisplayValue` — указать как `private computed` (секция 14)
5. **Добавить**注明 `private(set)` для свойства `expression` (секция 14)
6. **Дополнить** протоколы `HistoryEntry` до `Identifiable, Equatable, Codable, Sendable` (секция 10)
7. **Дополнить** протоколы `CalculatorError` до `Error, Equatable, LocalizedError, Sendable` (секция 10)
8. **Добавить примечание** о том, что Tests/ существует на диске, но не включено в SPM targets

---

## 7. Статистика верификации

| Категория | Всего утверждений | Верно | Неточно | Пропущено |
|---|---|---|---|---|
| Пути к файлам | 26 | 26 | 0 | 0 |
| Имена типов | 26 | 26 | 0 | 0 |
| Потокобезопасность | 10 | 10 | 0 | 0 |
| Архитектурные запреты | 4 | 4 | 0 | 0 |
| Методы ViewModel | 15 | 15 | 0 | 0 |
| Свойства ViewModel | 10 | 7 | 3 | 1 |
| Протоколы | 8 | 6 | 0 | 2 |
| Структура проекта | 12 | 11 | 0 | 1* |
| **ИТОГО** | **132** | **126** | **3** | **4** |

*Примечание: Tests/ — файлы существуют, но не в SPM targets

---

> **Заключение:** AGENTS.md является высококачественным документом. 95% утверждений верифицированы как точные. Найденные неточности носят косметический характер (видимости, типы) и не влияют на работоспособность агента при следовании инструкциям. Пропущенное свойство `memoryDisplayValue` — единственное существенное упущение.
