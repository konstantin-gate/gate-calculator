# Software Requirements Specification (SRS)

# Native Calculator for macOS

Version: 1.1 (Исправленная)

Status: Production Specification

Document Type: Software Requirements Specification (IEEE 29148 Inspired)

Target Platform: macOS

Primary Language: Swift 6

UI Framework: SwiftUI

---

# 1. Назначение документа

Настоящий документ определяет полный набор функциональных, нефункциональных, архитектурных, визуальных и эксплуатационных требований к разработке современного нативного приложения-калькулятора для macOS.

Документ одновременно является:

* техническим заданием;
* спецификацией требований;
* архитектурным ориентиром;
* системным промптом для AI-агента;
* критерием приемки проекта.

Любая реализация считается корректной только в том случае, если она удовлетворяет требованиям настоящего документа.

---

# 2. Роль AI-агента

Во время работы ты должен выступать одновременно в нескольких профессиональных ролях.

Каждая роль обязательна.

## Software Architect

Проектирует архитектуру приложения.

Несет ответственность за:

* структуру проекта;
* масштабируемость;
* сопровождаемость;
* простоту;
* модульность;
* расширяемость.

---

## Senior Swift Engineer

Пишет современный production-ready код.

Использует исключительно актуальные возможности языка Swift.

Следует Swift API Design Guidelines.

Следует рекомендациям Apple.

Не использует устаревшие практики.

---

## Senior SwiftUI Engineer

Создает полностью нативный интерфейс.

Использует только современные подходы SwiftUI.

Минимизирует использование AppKit.

Использует AppKit исключительно там, где SwiftUI пока объективно недостаточно.

---

## UX Designer

Каждое решение должно приниматься исходя из удобства пользователя.

Никаких случайных элементов.

Никаких необоснованных решений.

Каждый элемент существует только потому, что делает работу пользователя проще.

---

## Apple Human Interface Guidelines Expert

Все интерфейсные решения должны максимально соответствовать современному языку дизайна macOS.

При возникновении сомнений необходимо выбирать решение, наиболее соответствующее Apple Human Interface Guidelines.

Никогда не придумывать собственные паттерны поведения, если аналог уже существует в macOS.

---

## Performance Engineer

Следить за:

* производительностью;
* временем запуска;
* использованием памяти;
* отзывчивостью интерфейса;
* отсутствием блокировок главного потока.

---

## Code Reviewer

Постоянно анализировать собственный код.

Удалять:

* дублирование;
* лишние зависимости;
* ненужные абстракции;
* чрезмерную сложность.

---

## QA Engineer

После завершения каждого этапа самостоятельно искать ошибки.

Исправлять их до перехода к следующему этапу.

---

# 3. Главная цель проекта

Создать современный нативный калькулятор для macOS.

После запуска пользователь должен подумать:

> «Это выглядит как системный Calculator.app, но наконец-то умеет делать то, чего всегда не хватало.»

Именно такое впечатление является главной целью проекта.

---

# 4. Главная проблема, которую решает приложение

Современный системный Calculator.app в macOS имеет ограничение.

Пользователь не может просто вставить математическое выражение:

```
(15+16+17+18)/4
```

и сразу получить ответ.

Вместо этого приходится:

* вводить выражение вручную;
* использовать сторонние приложения;
* открывать инженерные калькуляторы;
* использовать блокноты для вычислений;
* использовать терминал.

Это ухудшает пользовательский опыт.

Настоящее приложение существует исключительно для устранения этого недостатка.

---

# 5. Философия приложения

Это НЕ математический пакет.

Это НЕ инженерный калькулятор.

Это НЕ язык программирования.

Это НЕ блокнот.

Это НЕ система компьютерной алгебры.

Это НЕ Soulvel.

Это НЕ Numi.

Это НЕ Excel.

Это НЕ Wolfram.

Это максимально простой повседневный калькулятор.

Если пользователь никогда не вставляет выражения из буфера обмена, он вообще не должен замечать отличий от системного Calculator.app.

---

# 6. Основные принципы

Все дальнейшие решения должны удовлетворять следующим принципам.

---

## Простота

Любая функция должна существовать только при наличии реальной пользовательской ценности.

Каждая новая возможность увеличивает сложность.

Если функция не улучшает повседневную работу пользователя, она должна быть исключена.

---

## Минимализм

Минимум настроек.

Минимум элементов.

Минимум отвлекающих факторов.

Минимум режимов.

---

## Предсказуемость

Пользователь должен заранее понимать результат любого действия.

Интерфейс не должен удивлять.

Он должен вести себя именно так, как ожидает пользователь macOS.

---

## Скорость

Каждое действие должно восприниматься мгновенным.

Целевое время реакции интерфейса:

менее 16 мс.

---

## Нативность

Каждый элемент интерфейса должен ощущаться частью macOS.

Запрещается создавать ощущение стороннего приложения.

---

## Последовательность

Во всем приложении используются единые правила:

* одинаковые анимации;
* одинаковые размеры;
* одинаковые интервалы;
* одинаковые принципы взаимодействия.

---

## Незаметность

Самое лучшее приложение — то, которое пользователь перестает замечать.

Он думает о вычислениях, а не об интерфейсе.

---

# 7. Scope проекта

Проект решает только одну задачу.

Быстро вычислять математические выражения.

Не более.

Все функции должны быть непосредственно связаны с этой задачей.

---

# 8. Что обязательно должно присутствовать (MUST)

Приложение обязательно должно поддерживать:

* сложение;
* вычитание;
* умножение;
* деление;
* проценты;
* отрицательные числа;
* десятичные дроби;
* вложенные скобки;
* длинные выражения;
* буфер обмена;
* горячие клавиши;
* историю вычислений;
* Light Mode;
* Dark Mode;
* локализацию;
* Accessibility;
* Retina;
* изменение размера окна;
* полноценную работу с клавиатуры.

Отсутствие любого из перечисленных пунктов считается несоответствием спецификации.

---

# 9. Что желательно поддерживать (SHOULD)

Приложение желательно должно поддерживать:

* Drag & Drop математических выражений;
* восстановление предыдущего состояния после перезапуска;
* красивую анимацию переходов;
* плавное масштабирование текста;
* адаптацию под различные размеры окна;
* сохранение положения окна.

---

# 10. Что допускается (MAY)

Допускается реализация:

* небольшой панели настроек;
* контекстного меню;
* меню приложения;
* минимальной истории вычислений;
* экспорта истории в текст.

Все подобные возможности должны быть полностью необязательными.

Они не должны усложнять основной пользовательский сценарий.

---

# 11. Что категорически запрещено

Запрещается добавлять:

* инженерный режим;
* научный режим;
* конвертер валют;
* конвертер единиц;
* тригонометрию;
* логарифмы;
* графики;
* программируемые функции;
* язык формул;
* макросы;
* AI-функции;
* облачную синхронизацию;
* регистрацию пользователей;
* рекламу;
* телеметрию;
* аналитические SDK;
* сторонние UI-фреймворки;
* Electron;
* WebView;
* Flutter;
* React Native;
* Qt;
* любые зависимости, не имеющие прямого отношения к основной задаче приложения.

Каждая лишняя функция ухудшает пользовательский опыт и противоречит философии проекта.

---

# 12. Критерий успеха проекта

Проект считается успешным, если пользователь сможет выполнить следующий сценарий без каких-либо инструкций:

1. Открыть приложение.
2. Нажать ⌘V.
3. Вставить:

```
(15+16+17+18)/4
```

4. Мгновенно увидеть правильный результат.
5. Закрыть приложение с ощущением, что именно так и должен работать системный калькулятор macOS.

Если этот сценарий выполняется естественно, быстро и без необходимости изучать интерфейс, главная цель проекта достигнута.

---

# 13. Технологический стек

Выбор технологий является частью архитектуры приложения.

Любая замена перечисленных ниже технологий требует веского технического обоснования.

Главный принцип:

**использовать исключительно современные нативные технологии Apple.**

---

## 13.1 Язык программирования

Обязательно использовать:

* Swift 6

Использовать исключительно современные возможности языка.

Следовать:

* Swift API Design Guidelines
* Swift Concurrency Best Practices

Избегать устаревших конструкций.

---

## 13.2 Пользовательский интерфейс

Основной UI-фреймворк:

* SwiftUI

Использовать современные API.

Использовать:

* Observation
* @Observable
* @Bindable
* @MainActor там, где это действительно необходимо.

Не использовать устаревший ObservableObject без объективной причины.

---

## 13.3 AppKit

AppKit допускается исключительно в случаях, когда SwiftUI пока не предоставляет полноценного решения.

Каждое использование AppKit должно иметь комментарий с объяснением причины.

Запрещается писать приложение целиком на AppKit.

---

## 13.4 Архитектурный стиль

Обязательно использовать MVVM.

Допускается применение небольших Coordinator-подобных компонентов при необходимости.

Не использовать чрезмерно сложные архитектуры.

Не применять VIPER.

Не применять Redux.

Не применять TCA, если его преимущества не оправдывают увеличение сложности проекта.

Главная цель — простая, понятная и легко сопровождаемая архитектура.

---

## 13.5 Зависимости

Минимизировать количество сторонних библиотек.

Предпочитать стандартные библиотеки Apple.

Каждая внешняя зависимость должна быть документирована и обоснована.

Если функциональность можно реализовать средствами Foundation или Swift Standard Library, использовать именно их.

---

# 14. Общие архитектурные принципы

Архитектура должна проектироваться с учетом длительной поддержки проекта.

Каждое решение должно отвечать на вопрос:

> "Будет ли этот код легко понимать через три года?"

Если ответ отрицательный — решение должно быть пересмотрено.

---

## Простота выше абстракции

Не создавать дополнительные уровни абстракции без необходимости.

Не создавать интерфейсы "на будущее".

Не проектировать функциональность, которой пока не существует.

Следовать принципу:

"You Aren't Gonna Need It (YAGNI)"

---

## Читаемость выше компактности

Предпочитать код, который легче читать.

Даже если он занимает больше строк.

---

## Явность выше магии

Избегать скрытого поведения.

Избегать сложных цепочек модификаторов.

Избегать трудно прослеживаемой логики.

Код должен быть очевидным.

---

## Композиция выше наследования

Предпочитать композицию.

Использовать value types.

Минимизировать использование классов.

---

## Локальность изменений

Изменение одной функции приложения не должно требовать изменений во множестве файлов.

Каждый модуль должен иметь четко определенную ответственность.

---

# 15. Обязательная последовательность разработки

Запрещается сразу приступать к написанию кода.

Разработка должна проходить поэтапно.

Переход между этапами допускается только после завершения предыдущего.

---

## Этап 1. Анализ требований

Перед написанием первой строки кода необходимо:

* полностью изучить настоящий документ;
* определить функциональные требования;
* определить нефункциональные требования;
* выявить потенциальные риски;
* выявить неоднозначности;
* предложить решения спорных моментов.

Если требования противоречат друг другу, реализация запрещается до устранения противоречий.

### Артефакты этапа 1:

1. **Таблица требований** (Excel или Markdown):
   - Столбец A: Номер требования (F-001, F-002, ...)
   - Столбец B: Описание требования
   - Столбец C: Приоритет (MUST / SHOULD / MAY)
   - Столбец D: Статус (подтверждено / требует уточнения)

2. **Список рисков**:
   - Риск 1: Неточности в приоритетах операторов → Решение: использовать стандартную математику
   - Риск 2: Сложности с форматированием чисел → Решение: использовать NumberFormatter
   - Риск 3: Производительность при длинных выражениях → Решение: тестировать на выражениях >100 токенов

3. **Устранённые неоднозначности**:
   - Проценты: используется математическая модель (деление на 100)
   - Числовой тип: обязателен Decimal
   - Вставка из буфера: всегда заменяет текущее выражение

---

## Этап 2. Проектирование архитектуры

До написания кода необходимо спроектировать:

* структуру проекта;
* взаимодействие модулей;
* поток данных;
* поток пользовательских событий;
* структуру моделей;
* структуру ViewModel;
* сервисы;
* вспомогательные компоненты;
* обработку ошибок;
* жизненный цикл приложения.

Каждое архитектурное решение должно сопровождаться объяснением.

### Артефакты этапа 2:

1. **Диаграмма компонентов** (Mermaid или ASCII):
```
[View] → [ViewModel] → [CalculatorEngine]
                         ↓
                    [HistoryService]
                         ↓
                    [ClipboardManager]
```

2. **Список модулей с ответственностью**:
   - `CalculatorEngine/Tokenizer`: преобразование строки в токены
   - `CalculatorEngine/Parser`: проверка синтаксиса и построение AST
   - `CalculatorEngine/Evaluator`: вычисление результата
   - `ViewModel/CalculatorViewModel`: управление состоянием UI
   - `Service/HistoryService`: хранение и получение истории
   - `Service/ClipboardManager`: работа с буфером обмена

3. **Схема потока данных**:
   - Пользователь вводит символ → ViewModel обновляет выражение → Tokenizer парсит → Parser проверяет → Evaluator вычисляет → ViewModel обновляет результат

---

## Этап 3. Проектирование UX

До реализации интерфейса необходимо описать:

первый запуск;

обычные вычисления;

вставку выражений;

копирование результата;

отображение ошибок;

работу клавиатуры;

работу мыши;

историю;

настройки.

Все пользовательские сценарии должны быть непротиворечивыми.

### Артефакты этапа 3:

1. **User Flow Diagram** (Mermaid):
```
Начало → Открытие приложения → Ожидание ввода
       ↓
Ввод с клавиатуры → Обновление дисплея → Вычисление (по Enter)
       ↓
Вставка из буфера → Замена выражения → Вычисление (автоматически)
       ↓
Нажатие кнопки → Обновление дисплея → Вычисление (по "=")
```

2. **Сценарий "Первый запуск"**:
   - Шаг 1: Пользователь открывает приложение
   - Шаг 2: Видит пустой дисплей и кнопки
   - Шаг 3: Может сразу вводить или вставлять выражение
   - Ожидаемое время до готовности: <100 мс

3. **Сценарий "Вставка выражения"**:
   - Шаг 1: Пользователь нажимает ⌘V
   - Шаг 2: Выражение появляется на дисплее
   - Шаг 3: Автоматически вычисляется
   - Шаг 4: Результат отображается
   - Ожидаемое время: <50 мс

---

## Этап 4. Проектирование вычислительного движка

До написания вычислителя необходимо описать:

структуру токенов;

алгоритм парсинга;

обработку ошибок;

приоритет операций;

ассоциативность операторов;

обработку отрицательных чисел;

обработку процентов;

алгоритм вычисления.

### Артефакты этапа 4:

1. **Схема токенов** (enum):
```swift
enum Token {
    case number(Decimal)
    case operator(BinaryOperator)
    case unaryMinus
    case leftParenthesis
    case rightParenthesis
    case percent
}

enum BinaryOperator {
    case add
    case subtract
    case multiply
    case divide
}
```

2. **Алгоритм парсинга** (пошагово):
   - Шаг 1: Tokenizer разбивает строку на токены
   - Шаг 2: Parser проверяет синтаксическую корректность
   - Шаг 3: Parser строит AST (дерево выражений)
   - Шаг 4: Evaluator обходит дерево и вычисляет результат

3. **Обработка ошибок** (таблица):
   | Ошибка | Сообщение пользователю | Код ошибки |
   |--------|----------------------|------------|
   | Лишняя закрывающая скобка | "Пропущена открывающая скобка" | ERR_PAREN_01 |
   | Незакрытая скобка | "Пропущена закрывающая скобка" | ERR_PAREN_02 |
   | Деление на ноль | "Деление на ноль" | ERR_DIV_ZERO |
   | Неизвестный символ | "Неверное выражение" | ERR_INVALID_CHAR |

---

## Этап 5. Проектирование интерфейса

До написания SwiftUI-кода необходимо описать:

структуру окна;

структуру экранов;

расположение элементов;

анимации;

цветовую систему;

типографику;

реакции элементов;

адаптацию интерфейса.

### Артефакты этапа 5:

1. **Макет окна** (ASCII art):
```
┌─────────────────────────┐
│                         │
│      Дисплей            │
│   (выражение + результат)│
│                         │
├─────────────────────────┤
│  C   (   )   ÷          │
│  7   8   9   ×          │
│  4   5   6   -          │
│  1   2   3   +          │
│  0 (на 2 колонки)  =    │
└─────────────────────────┘
```

2. **Список SwiftUI-компонентов**:
   - `CalculatorView`: главный контейнер
   - `DisplayView`: отображение выражения и результата
   - `ButtonRowView`: ряд кнопок
   - `CalculatorButton`: отдельная кнопка
   - `HistoryPanelView`: панель истории (popover)

3. **Цветовая схема** (SwiftUI):
```swift
struct CalculatorColors {
    static let background = Color(.systemGroupedBackground)
    static let buttonDigit = Color(.secondarySystemBackground)
    static let buttonOperator = Color(.systemGray5)
    static let buttonEquals = Color(.systemBlue)
    static let textPrimary = Color(.primaryLabel)
    static let textSecondary = Color(.secondaryLabel)
}
```

---

## Этап 6. План реализации

Проект должен быть разбит на независимые этапы.

После завершения каждого этапа необходимо:

* провести рефакторинг;
* удалить дублирование;
* проверить архитектуру;
* проверить производительность;
* проверить соответствие спецификации.

---

## Этап 7. Реализация

Только после выполнения предыдущих этапов допускается написание кода.

Во время реализации необходимо:

писать небольшими законченными частями;

не оставлять временные решения;

не оставлять TODO;

не оставлять Mock;

не оставлять заглушки;

не писать код "на потом".

---

## Этап 8. Финальный аудит

После завершения реализации необходимо полностью проверить проект.

Если выявлены недостатки — устранить их.

Только после этого проект считается завершенным.

---

# 16. Структура проекта

Рекомендуемая структура каталогов:

```text
Calculator/

    App/

    Views/

    ViewModels/

    Models/

    Services/

    CalculatorEngine/

        Tokenizer/

        Parser/

        Evaluator/

        AST/

    Clipboard/

    History/

    Formatting/

    Theme/

    Components/

    Extensions/

    Resources/

    Localization/

    Tests/

        Unit/

        UI/

```

Каждый каталог должен содержать только файлы своей ответственности.

Запрещается смешивать бизнес-логику и пользовательский интерфейс.

---

# 17. Правила организации кода

Каждый файл должен иметь единственную ответственность.

Не создавать файлы длиной в несколько тысяч строк.

Предпочтительный размер файла:

100–300 строк.

Допускается больше только при объективной необходимости.

---

View не должен содержать бизнес-логику.

---

ViewModel не должен заниматься вычислениями.

---

Parser не должен знать о пользовательском интерфейсе.

---

Evaluator не должен знать о SwiftUI.

---

Clipboard Manager не должен знать о Parser.

---

History не должен знать о View.

---

Theme не должен знать о Calculator Engine.

---

Все зависимости должны быть направлены в одну сторону.

---

# 18. Dependency Management

Каждый модуль должен иметь минимальное количество зависимостей.

Предпочитать Dependency Injection.

Не использовать глобальные Singleton без объективной необходимости.

Если Singleton действительно необходим — его использование должно быть обосновано.

---

# 19. Обработка ошибок

Ошибки являются частью пользовательского опыта.

Ошибка никогда не должна приводить к:

* падению приложения;
* зависанию интерфейса;
* повреждению состояния приложения.

Все ошибки должны быть:

предсказуемыми;

локализованными;

понятными пользователю.

Технические детали не должны отображаться пользователю.

---

# 20. Код-стайл

Следовать:

Swift API Design Guidelines.

Использовать:

понятные имена;

небольшие функции;

небольшие структуры;

небольшие перечисления.

Избегать:

магических чисел;

магических строк;

неочевидных сокращений.

Каждая публичная сущность должна иметь понятную документацию.

---

# Acceptance Criteria

Архитектура считается соответствующей требованиям, если:

* отсутствуют циклические зависимости;
* каждый модуль имеет одну ответственность;
* структура проекта понятна без дополнительной документации;
* новый разработчик способен разобраться в архитектуре менее чем за один рабочий день;
* добавление новой функции требует минимального количества изменений в существующем коде.

---

# 21. Вычислительный движок (Calculator Engine)

## Назначение

Calculator Engine является ядром приложения.

Он отвечает исключительно за:

* разбор математического выражения;
* проверку корректности;
* вычисление результата;
* возврат результата или структурированной ошибки.

Engine не должен содержать:

* пользовательский интерфейс;
* зависимости от SwiftUI;
* зависимости от AppKit;
* работу с буфером обмена;
* работу с историей;
* форматирование отображения.

Engine должен быть полностью независимым модулем.

В идеале он должен собираться и тестироваться отдельно от остального приложения.

---

# 22. Архитектура Calculator Engine

Рекомендуемая структура:

```text
CalculatorEngine/

    Tokenizer/

        Token.swift

        Tokenizer.swift

    Parser/

        Parser.swift

        Operator.swift

        Precedence.swift

    AST/

        ExpressionNode.swift

    Evaluator/

        Evaluator.swift

    Errors/

        CalculatorError.swift

```

Каждый модуль имеет одну ответственность.

---

# 23. Требования к вычислительному движку

Engine обязан:

* работать детерминированно;
* быть полностью повторяемым;
* не использовать глобальное состояние;
* быть потокобезопасным;
* не хранить внутреннее состояние между вычислениями.

Каждый вызов вычисления должен быть независимым.

---

# 24. Парсер

## Запрещается использовать

Категорически запрещается использовать:

* NSExpression;
* JavaScriptCore;
* WebKit;
* eval;
* сторонние математические библиотеки;
* сторонние Expression Engine.

Причины:

* ограниченная расширяемость;
* плохой контроль ошибок;
* потенциальные проблемы безопасности;
* зависимость от внутренних реализаций Apple.

---

## Требование

Парсер должен быть полностью собственной реализацией.

---

# 25. Рекомендуемый алгоритм

Допускаются два варианта.

## Вариант 1 (предпочтительный)

Tokenizer

↓

Shunting Yard

↓

Reverse Polish Notation

↓

Evaluator

Преимущества:

* простой;
* быстрый;
* легко тестируется;
* хорошо поддерживает приоритет операторов.

---

## Вариант 2

Tokenizer

↓

Recursive Descent Parser

↓

AST

↓

Evaluator

Допускается, если реализация получается проще и чище.

---

# 26. Tokenizer

Tokenizer обязан преобразовывать строку пользователя в последовательность токенов.

Поддерживаемые токены:

Number

Operator

Left Parenthesis

Right Parenthesis

Percent

Unary Minus

Decimal Separator

Whitespace

Whitespace должен игнорироваться.

---

# 27. Поддерживаемые операторы

Обязательно поддерживаются:

+ (сложение)
- (вычитание)
* (умножение)
/ (деление)
% (процент)
( (открывающая скобка)
) (закрывающая скобка)
Унарный минус (для отрицательных чисел)

---

# 28. Приоритет операций

Наивысший приоритет:
- Вложенные скобки ()

Следующий уровень (левый приоритет):
- Унарный минус (для отрицательных чисел)

Следующий уровень (левый приоритет):
- * (умножение)
- / (деление)
- % (процент)

Низший уровень (левый приоритет):
- + (сложение)
- - (вычитание)

Все операторы одного уровня имеют левую ассоциативность.

---

# 29. Поддерживаемые выражения

Обязательно поддерживаются:

```text
15+16
```

```text
15+16*5
```

```text
(15+16)*5
```

```text
((15+16)*5)/3
```

```text
-5+12
```

```text
-(15+16)
```

```text
(-15+6)
```

```text
3.1415*5
```

```text
15 + 16 + 17 + 18
```

```text
(15 +16+17 ) /4
```

Любая комбинация пробелов должна работать одинаково.

---

# 30. Обработка пробелов

Пробелы никогда не должны влиять на результат.

Все следующие варианты эквивалентны:

```text
15+16
```

```text
15 +16
```

```text
15+ 16
```

```text
15 + 16
```

```text
 15 + 16
```

```text
15 + 16
```

Tokenizer обязан игнорировать пробелы.

---

# 31. Десятичные числа

Поддерживаются:

```text
3.14
```

```text
0.5
```

```text
-0.25
```

Желательно учитывать локаль пользователя при вводе и отображении десятичного разделителя (например, запятая или точка), сохраняя внутреннее представление независимым от локали.

---

# 32. Проценты

## Модель работы с процентами

Процент (%) обрабатывается как бинарный оператор с тем же приоритетом, что и умножение (*), деление (/).

## Правила вычисления:

1. **Процент после числа используется как деление на 100:**
   - `5%` = `0.05`
   - `100%` = `1.0`

2. **Процент вычисляется слева направо с другими операторами одинакового приоритета:**
   - `100 * 5%` = `100 * 0.05` = `5.0`
   - `100 / 5%` = `100 / 0.05` = `2000.0`
   - `100 + 5%` = `100 + 0.05` = `100.05`
   - `100 - 5%` = `100 - 0.05` = `99.95`

3. **Процент может быть унарным оператором (после числа):**
   - `50 + 10%` = `50 + (50 * 0.10)` = `55.0` (в контексте калькулятора)
   - Но в математическом выражении: `50 + 10%` = `50 + 0.10` = `50.10`

## Выбор модели:

Для данного проекта используется **математическая модель** (правила 1-3):
- Процент всегда означает "делить на 100"
- Не используется модель "процент от предыдущего числа"

## Примеры для тестирования:

```
50% = 0.5
100 * 5% = 5.0
100 / 5% = 2000.0
(50 + 10) % = 60% = 0.6
```

## Обработка ошибок:

Если процент используется некорректно (например, `%*` или `*%`), парсер должен вернуть ошибку "Некорректное использование оператора %".

---

# 33. Ошибки парсинга

Парсер обязан корректно обнаруживать:

лишнюю закрывающую скобку;

незакрытую скобку;

неизвестный символ;

двойной оператор;

пустое выражение;

некорректное число;

несколько десятичных разделителей;

деление на ноль;

неожиданный конец выражения.

Ошибки должны быть структурированными.

---

# 34. Отображение ошибок

Пользователь никогда не должен видеть:

stack trace;

debug information;

Swift Error;

NSError.

Пользователь должен видеть понятные сообщения.

Например:

"Неверное выражение"

"Пропущена закрывающая скобка"

"Деление на ноль"

"Неожиданный символ"

Сообщения должны быть локализуемыми.

---

# 35. Требования к обработке ошибок

## Общие принципы:

1. **Никогда не падать**:
   - Приложение не должно аварийно завершаться ни при каком вводе
   - Все ошибки должны быть перехвачены и обработаны

2. **Никогда не блокировать UI**:
   - Ошибки не должны вызывать зависание интерфейса
   - Все вычисления должны быть синхронными (они быстрые)

3. **Понятные сообщения**:
   - Пользователь должен понимать, что пошло не так
   - Технические детали не отображаются

## Классификация ошибок:

### Критические ошибки (красный цвет, alert):
- Деление на ноль: "Деление на ноль"
- Переполнение: "Число слишком большое"

### Ошибки парсинга (жёлтый цвет, inline):
- Некорректное выражение: "Неверное выражение"
- Пропущена скобка: "Пропущена закрывающая скобка"
- Лишняя скобка: "Лишняя закрывающая скобка"

### Предупреждения (серый цвет, inline):
- Неверный символ: "Неверное выражение"
- Некорректная вставка: "Невозможно вычислить"

## Реализация:

В CalculatorError добавить все возможные ошибки:

```swift
enum CalculatorError: Error, LocalizedError {
    case divisionByZero
    case invalidExpression(String) // "Неверное выражение"
    case missingClosingParenthesis
    case extraClosingParenthesis
    case invalidCharacter(String) // "Неверный символ: X"
    case emptyExpression
    case numberOverflow
    case invalidNumber(String) // "Некорректное число: X"
    
    var errorDescription: String? {
        switch self {
        case .divisionByZero:
            return "Деление на ноль"
        case .invalidExpression(let msg):
            return msg
        case .missingClosingParenthesis:
            return "Пропущена закрывающая скобка"
        case .extraClosingParenthesis:
            return "Лишняя закрывающая скобка"
        case .invalidCharacter(let char):
            return "Неверный символ: \(char)"
        case .emptyExpression:
            return "Выражение пустое"
        case .numberOverflow:
            return "Число слишком большое"
        case .invalidNumber(let num):
            return "Некорректное число: \(num)"
        }
    }
}
```

## Отображение ошибок в UI:

В DisplayView добавить метод:

```swift
func showError(_ error: CalculatorError) {
    // 1. Анимировать появление сообщения
    // 2. Изменить цвет на красный/жёлтый
    // 3. Показать сообщение об ошибке вместо результата
    // 4. Через 2 секунды автоматически скрыть (опционально)
}

func clearError() {
    // 1. Скрыть сообщение об ошибке
    // 2. Вернуть цвет к нормальному
}
```

## Тестирование:

Написать тесты для всех ошибок:

```swift
func testError_DivisionByZero() {
    let result = try? CalculatorEngine.evaluate("1/0")
    XCTAssertNil(result)
}

func testError_InvalidCharacter() {
    let result = try? CalculatorEngine.evaluate("15+a")
    XCTAssertNil(result)
}

func testError_MissingParenthesis() {
    let result = try? CalculatorEngine.evaluate("(15+16")
    XCTAssertNil(result)
}

func testError_ExtraParenthesis() {
    let result = try? CalculatorEngine.evaluate("15+16)")
    XCTAssertNil(result)
}
```

## Локализация ошибок:

Все сообщения об ошибках должны быть вынесены в локализуемые строки:

```swift
// Localizable.strings (English)
"error.divisionByZero" = "Division by zero"
"error.invalidExpression" = "Invalid expression"
"error.missingParenthesis" = "Missing closing parenthesis"

// Localizable.strings (Russian)
"error.divisionByZero" = "Деление на ноль"
"error.invalidExpression" = "Неверное выражение"
"error.missingParenthesis" = "Пропущена закрывающая скобка"
```

---

# 36. Числовая модель

## Обязательное использование Decimal

Для всех вычислений в калькуляторе обязательно использовать тип `Decimal` из модуля Foundation.

## Причины выбора:

1. **Точность:** `Decimal` не имеет ошибок округления, характерных для `Double`:
   - `0.1 + 0.2` с `Decimal` = `0.3` (правильно)
   - `0.1 + 0.2` с `Double` = `0.30000000000000004` (ошибка)

2. **Предсказуемость:** Пользователь всегда получает ожидаемый результат без артефактов

3. **Производительность:** Для калькулятора разница в скорости между `Decimal` и `Double` незаметна (менее 1 мс)

## Исключения:

Использование `Double` допускается только в следующих случаях:
- Внутренние вычисления, не отображаемые пользователю
- Работа с системными API, требующими `Double`

Все такие случаи должны быть документированы в коде.

## Форматирование:

При отображении результатов использовать `NumberFormatter` с учётом локали пользователя.
Удалять незначащие нули (например, `15.0` отображать как `15`).

## Примеры:

```swift
// Правильно
let result = Decimal(string: "15.5")! + Decimal(string: "20.3")!

// Неправильно
let result = 15.5 + 20.3
```

---

# 37. Требования к форматированию чисел

## Общие правила:

1. **Удаление незначащих нулей**:
   - `15.0` → отображать как `15`
   - `15.50` → отображать как `15.5`
   - `0.500` → отображать как `0.5`

2. **Разделители тысяч**:
   - По умолчанию включены (если в системных настройках включено)
   - `1000` → `1,000`
   - `1000000` → `1,000,000`
   - Можно отключить в настройках

3. **Максимальное количество знаков после запятой**:
   - По умолчанию: 10 знаков
   - Можно настроить в настройках (от 0 до 20)
   - Если результат короче → отображать без лишних нулей

4. **Экспоненциальная запись**:
   - Использовать для чисел < 0.000001 или > 999999999999
   - `0.0000001` → `1e-7`
   - `1000000000000` → `1e12`

## Реализация:

Использовать `NumberFormatter`:

```swift
class NumberFormatterService {
    static let shared = NumberFormatterService()
    
    private let formatter: NumberFormatter
    
    init() {
        formatter = NumberFormatter()
        formatter.numberStyle = .decimal
        formatter.maximumFractionDigits = 10
        formatter.minimumFractionDigits = 0
        formatter.groupingSeparator = ","
        formatter.decimalSeparator = "."
        formatter.allowsFloats = true
    }
    
    func format(_ value: Decimal) -> String {
        // 1. Проверить, нужно ли использовать экспоненциальную запись
        // 2. Скорректировать максимальное количество знаков
        // 3. Отформатировать и вернуть строку
    }
}
```

## Обработка очень больших чисел:

Для чисел > 10^21 (предел точности Decimal):
- Отобразить в экспоненциальной записи
- Показать предупреждение "Число слишком большое для точного отображения"

## Обработка очень маленьких чисел:

Для чисел < 10^-21:
- Отобразить в экспоненциальной записи

## Локализация:

- Использовать текущую локаль системы для разделителей
- В русской локали: `1,000.5` → `1 000,5` (пробел как разделитель тысяч, запятая как десятичная)
- В английской локали: `1,000.5` → `1,000.5`

## Примеры форматирования:

| Decimal значение | Отображение (EN) | Отображение (RU) |
|-----------------|------------------|------------------|
| `15` | `15` | `15` |
| `15.0` | `15` | `15` |
| `15.5` | `15.5` | `15,5` |
| `1000` | `1,000` | `1 000` |
| `0.0000001` | `1e-7` | `1e-7` |
| `1000000000000` | `1e12` | `1e12` |

---

# 38. Буфер обмена

Поддержка буфера обмена является одной из ключевых функций приложения.

Она должна ощущаться абсолютно естественной.

---

## Вставка выражения

Если пользователь нажимает:

⌘V

или выбирает:

Edit → Paste

или использует контекстное меню,

и в буфере находится корректное математическое выражение, приложение должно:

1. вставить выражение;
2. немедленно распознать его;
3. вычислить;
4. обновить дисплей.

Никаких дополнительных действий не требуется.

---

## Поддерживаемые варианты

Все перечисленные варианты должны работать:

```text
15+16
```

```text
15 +16
```

```text
15+ 16
```

```text
15 + 16
```

```text
((15+16)*4)/7
```

```text
-(15+16)
```

---

## Если вставлено число

Если в буфере находится только число:

```text
125
```

то оно должно просто стать текущим значением.

---

## Если вставлен мусор

Если вставлен текст:

```text
Hello World
```

или

```text
Apple Inc.
```

или произвольная строка, не являющаяся математическим выражением, приложение не должно аварийно завершаться.

Оно должно:

* проигнорировать вставку;
* либо показать локализованное сообщение о невозможности вычисления.

Поведение должно быть единообразным и документированным.

---

## Drag & Drop

Желательно поддерживать перетаскивание текстового выражения непосредственно в окно приложения.

Если перетащенный объект содержит допустимое выражение, оно должно быть обработано так же, как при вставке через буфер обмена.

---

# 39. Обработка специальных случаев ввода

## Общие правила:

1. **Валидация на лету**:
   - После каждого ввода (с клавиатуры или из буфера) проверять корректность выражения
   - Если выражение некорректно → отображать ошибку, но не прерывать ввод
   - Если выражение корректно → вычислять и отображать результат

2. **Изоляция ошибок**:
   - Ошибка в текущем выражении не должна влиять на историю
   - Ошибка не должна блокировать следующий ввод
   - После ошибки пользователь может сразу начать новое выражение

3. **Восстановление после ошибки**:
   - После отображения ошибки клавиша "C" (Clear) сбрасывает всё в исходное состояние
   - После отображения ошибки клавиша Backspace удаляет последний символ и пытается вычислить заново

## Таблица специальных случаев:

| Ввод | Ожидаемый результат | Действие |
|------|-------------------|----------|
| `(` | Ошибка "Пропущена закрывающая скобка" (если выражение завершено) | Продолжить ввод |
| `)` без `(` | Ошибка "Лишняя закрывающая скобка" | Продолжить ввод |
| `5 +` (оператор в конце) | Ожидание следующего операнда | Продолжить ввод |
| `5 ++ 3` | Ошибка "Некорректное выражение" | Показать ошибку |
| `5 * -3` | `-15` (унарный минус после бинарного оператора) | Вычислить |
| `0.1 + 0.2` | `0.3` (без артефактов благодаря Decimal) | Вычислить |
| `1/0` | Ошибка "Деление на ноль" | Показать ошибку |
| `5 %` (процент в конце) | `0.05` (5 / 100) | Вычислить |
| `5% + 10` | `0.05 + 10 = 10.05` | Вычислить |
| `π` | `3.14159265358979` | Вычислить |
| `e` | `2.71828182845905` | Вычислить |
| `π * 2` | `6.28318530717958` | Вычислить |
| `0xFF` | `255` | Вычислить (hex) |
| `0b1010` | `10` | Вычислить (binary) |
| `0o77` | `63` | Вычислить (octal) |
| `1.5e3` | `1500` | Вычислить (экспоненциальная запись) |
| `1,000,000` | `1000000` (разделитель тысяч) | Вычислить |
| `1,5` (в локали с запятой) | `1.5` (десятичный разделитель) | Вычислить |
| `NaN` | Ошибка "Неверное выражение" | Показать ошибку |
| `Infinity` | Ошибка "Неверное выражение" | Показать ошибку |
| Пустая строка в буфере | Игнорировать вставку | Без действий |
| `Hello World` | Игнорировать вставку или показать ошибку | Показать ошибку |
| `15+16\n20+30` | Обработать только `15+16` = `31` | Игнорировать вторую строку |
| `(15+16)/4=` | Обработать как `(15+16)/4` = `7.75` | Удалить "=" |

## Реализация валидации:

В CalculatorViewModel добавить метод `validate(expression: String) -> ValidationResult`:

```swift
enum ValidationResult {
    case valid
    case invalid(String) // сообщение об ошибке
    case incomplete // выражение неполное, ожидает продолжения
}

func validate(expression: String) -> ValidationResult {
    // 1. Проверить баланс скобок
    // 2. Проверить корректность операторов
    // 3. Проверить корректность чисел
    // 4. Проверить наличие зарезервированных слов (NaN, Infinity)
    // 5. Проверить корректность процентов
}
```

## Реализация в Tokenizer:

В Tokenizer добавить методы:

```swift
class Tokenizer {
    func tokenize(_ input: String) -> [Token] {
        // 1. Удалить "=" в конце, если есть
        // 2. Разбить по переносам строк, взять первую строку
        // 3. Удалить запятые (разделители тысяч) или конвертировать в точки
        // 4. Поддержать экспоненциальную запись (e/E)
        // 5. Поддержать hex/binary/octal префиксы
        // 6. Поддержать константы π и e
    }
}
```

## Реализация в Parser:

В Parser добавить методы:

```swift
class Parser {
    func parse(_ tokens: [Token]) throws -> ExpressionNode {
        // 1. Проверить баланс скобок
        // 2. Проверить корректность последовательности операторов
        // 3. Построить AST
        // 4. Вернуть корень дерева или ошибку
    }
}
```

## Реализация в Evaluator:

В Evaluator добавить методы:

```swift
class Evaluator {
    func evaluate(_ node: ExpressionNode) throws -> Decimal {
        // 1. Рекурсивно обойти дерево
        // 2. Выполнить вычисления с Decimal
        // 3. Проверить деление на ноль
        // 4. Вернуть результат или ошибку
    }
}
```

## Тестирование специальных случаев:

Для каждого случая из таблицы выше написать Unit Test:

```swift
func testSpecialCase_01_DivisionByZero() {
    let result = try? CalculatorEngine.evaluate("1/0")
    XCTAssertNil(result) // должна быть ошибка
}

func testSpecialCase_02_HexNumber() {
    let result = try CalculatorEngine.evaluate("0xFF")
    XCTAssertEqual(result, 255)
}

func testSpecialCase_03_ExponentialNotation() {
    let result = try CalculatorEngine.evaluate("1.5e3")
    XCTAssertEqual(result, 1500)
}

// ... и так далее для всех случаев
```

---

# Acceptance Criteria

Подсистема вычислений считается соответствующей требованиям, если:

* выражения любой разумной длины вычисляются корректно;
* пробелы не влияют на результат;
* вложенные скобки работают без ограничений, кроме разумных технических пределов;
* все синтаксические ошибки обнаруживаются и отображаются корректно;
* вычисления не вызывают зависаний интерфейса;
* пользователь может вставить выражение через ⌘V и получить результат без дополнительных действий;
* формат результата соответствует ожиданиям обычного пользователя, а не внутреннему представлению чисел.

---

# 40. Пользовательский интерфейс (User Interface)

## Общая философия

Пользовательский интерфейс является наиболее важной частью данного проекта.

Главная задача интерфейса — не демонстрировать возможности приложения, а полностью исчезнуть из сознания пользователя.

Пользователь должен думать исключительно о вычислениях.

Не об интерфейсе.

Не о дизайне.

Не о приложении.

Интерфейс должен ощущаться естественным продолжением macOS.

---

# 41. Главная цель дизайна

После первого запуска пользователь должен испытать примерно следующее ощущение:

> «Это выглядит именно так, как должен выглядеть современный Calculator.app.»

Не:

> «Интересный сторонний калькулятор.»

Не:

> «Красивый дизайнерский концепт.»

Не:

> «Клон Calculator.»

А именно:

> «Это ощущается как приложение Apple.»

Это является одним из главных критериев качества проекта.

---

# 42. Обязательное правило проектирования интерфейса

## Конкретные спецификации интерфейса

### Размеры окна (в пунктах):
- Минимальная ширина: 320 pt
- Максимальная ширина: 400 pt
- Минимальная высота: 500 pt
- Максимальная высота: 600 pt
- Рекомендуемый размер по умолчанию: 350 x 520 pt

### Размеры кнопок:
- Цифровые кнопки: квадратные, сторона 60 pt (при стандартном размере окна)
- Кнопки операций: прямоугольные, ширина 60 pt, высота 60 pt
- Кнопка "=": прямоугольная, ширина 132 pt, высота 60 pt
- Кнопка "C": квадратная, сторона 60 pt

### Отступы:
- Внутренний отступ дисплея: 20 pt сверху, 10 pt снизу
- Отступ между рядами кнопок: 8 pt
- Отступ между колонками кнопок: 8 pt
- Внешний отступ калькулятора: 16 pt

### Радиусы скругления:
- Кнопки: 12 pt
- Дисплей: 16 pt

### Типографика дисплея:
- Текущее выражение: SF Pro, 18 pt, цвет secondaryLabel
- Результат: SF Pro, 42 pt, цвет primaryLabel
- Ошибка: SF Pro, 24 pt, цвет systemRed

### Цвета (системные):
- Фон калькулятора: systemGroupedBackground
- Фон дисплея: clear
- Фон цифровых кнопок: secondarySystemBackground
- Фон кнопок операций: systemGray5
- Фон кнопки "=": systemBlue
- Текст на цифровых кнопках: primaryLabel
- Текст на кнопках операций: primaryLabel
- Текст на кнопке "=": white

### Анимации:
- Нажатие кнопки: scale 0.95, длительность 0.1 с
- Появление результата: fade in, длительность 0.2 с
- Переход между состояниями: ease-in-out, длительность 0.15 с

### Источник спецификаций:
Все размеры и цвета должны соответствовать Apple Human Interface Guidelines для macOS.
При возникновении сомнений использовать системные API:
- `NSApplication.shared` для получения системных цветов
- `Font.systemFont(ofSize:)` для получения системных шрифтов
- `Color(nsColor:)` для конвертации в SwiftUI

### Проверка соответствия:

После реализации интерфейса необходимо сравнить с системным Calculator.app:
- Открыть оба приложения рядом
- Убедиться, что размеры, отступы и цвета визуально совпадают
- При расхождениях скорректировать до соответствия HIG

---

# 43. Что необходимо воспроизвести

Необходимо максимально точно воспроизвести именно визуальный язык современной macOS.

Следует стремиться к совпадению:

ритма;

визуального веса;

баланса;

контраста;

спокойствия интерфейса;

ощущения материалов;

анимаций;

отзывчивости;

микровзаимодействий.

---

# 44. Что запрещается копировать

Запрещается:

использовать изображения Apple;

копировать ассеты;

использовать системные ресурсы не по назначению;

копировать внутренние изображения Calculator.app;

извлекать элементы интерфейса из приложения Apple.

---

# 45. Что необходимо сделать вместо этого

Необходимо самостоятельно воссоздать тот же визуальный язык, используя:

SwiftUI;

SF Symbols;

SF Pro;

системные цвета;

Material;

официальные API macOS.

Если поведение какого-либо элемента неизвестно, необходимо выбрать решение, максимально соответствующее Apple Human Interface Guidelines.

Запрещается придумывать собственный стиль без веской причины.

---

# 46. Общий стиль

Интерфейс должен быть:

современным;

минималистичным;

спокойным;

дорогим;

визуально лёгким;

чистым;

аккуратным;

профессиональным.

Не должен производить впечатление экспериментального дизайнерского проекта.

---

# 47. Категорически запрещённые стили

Запрещается использовать:

Material Design;

Fluent Design;

Windows UI;

Android UI;

Neumorphism;

декоративный Glassmorphism;

Skeuomorphism;

ретро-стили;

игровой интерфейс;

яркие декоративные эффекты;

необоснованные градиенты;

анимации ради анимаций.

Единственный допустимый источник вдохновения — современный язык дизайна macOS.

---

# 48. Размер окна

Размер окна должен быть близок к системному Calculator.app.

Окно должно ощущаться компактным.

Не перегруженным.

Не вытянутым.

Не квадратным без необходимости.

Минимальный размер должен гарантировать:

полную читаемость;

удобное попадание по кнопкам;

отсутствие наложения элементов.

Максимальный размер также должен быть ограничен.

Нет смысла растягивать калькулятор на весь экран.

При увеличении окна элементы должны масштабироваться аккуратно, без ощущения пустоты или неестественно больших кнопок.

---

# 49. Поведение окна

Поддерживается:

Light Mode

Dark Mode

Stage Manager

Mission Control

несколько рабочих столов

Retina

изменение размера

восстановление положения окна

восстановление размеров окна

---

# 50. Основная композиция

Интерфейс состоит из трёх основных зон.

## Верхняя зона

Дисплей.

---

## Средняя зона

Дополнительная информация.

Например:

ошибка;

предыдущее выражение;

небольшой индикатор истории.

Эта область не должна отвлекать внимание.

---

## Нижняя зона

Кнопки калькулятора.

Именно они являются главным интерактивным элементом.

---

# 51. Дисплей

Дисплей является центральным элементом приложения.

Он должен визуально доминировать.

Но не выглядеть тяжёлым.

На дисплее отображается:

текущее выражение;

результат;

ошибки.

Не следует использовать несколько отдельных окон.

Всё должно происходить внутри одного дисплея.

---

# 52. Поведение дисплея

Во время ввода пользователь всегда видит выражение.

После вычисления результат отображается максимально понятно.

Если выражение длинное —

дисплей автоматически адаптируется.

Например:

уменьшает размер шрифта;

позволяет горизонтальную прокрутку;

или использует другое нативное решение.

Главное —

информация никогда не должна обрезаться неожиданным образом.

---

# 53. Типографика

Использовать исключительно системные шрифты.

SF Pro.

Размеры должны автоматически адаптироваться.

Вес шрифта должен соответствовать важности информации.

Результат должен восприниматься визуально важнее выражения.

---

# 54. Цветовая система

Использовать исключительно системные цвета.

Никаких жёстко заданных RGB.

Использовать semantic colors.

Интерфейс обязан автоматически адаптироваться к:

Light Mode

Dark Mode

повышенному контрасту

доступности.

---

# 55. Материалы

Использовать современные системные материалы там, где это оправдано.

Не использовать декоративное размытие ради красоты.

Материал должен помогать восприятию интерфейса.

Не отвлекать.

---

# 56. Кнопки

Кнопки должны ощущаться полностью нативными.

Каждая кнопка имеет:

обычное состояние;

hover;

pressed;

disabled (если применимо);

keyboard focus.

Переходы между состояниями должны быть плавными.

---

# 57. Размер кнопок

Все цифровые кнопки имеют одинаковый размер.

Кнопки операций имеют одинаковую систему размеров.

Попадание мышью должно быть максимально удобным.

Не следует делать кнопки меньше рекомендуемых Apple размеров.

---

# 58. Скругления

Использовать современные радиусы.

Избегать слишком острых углов.

Избегать чрезмерно круглых форм.

Все радиусы должны принадлежать единой дизайн-системе.

Не допускается использование разных радиусов без причины.

---

# 59. Отступы

Все интервалы должны строиться на единой модульной сетке.

Не должно существовать случайных расстояний между элементами.

Рекомендуется использовать кратные значения базового шага (например, 4 или 8 пунктов), если это соответствует общей композиции интерфейса.

---

# 60. Анимации

Все анимации должны иметь практическую цель.

---

# 61. Hover

Наведение курсора должно ощущаться естественным.

Не использовать чрезмерное изменение размеров.

Не использовать яркие свечения.

Допускаются:

небольшое изменение материала;

изменение яркости;

изменение непрозрачности;

едва заметное изменение тени.

---

# 62. Pressed State

Во время нажатия кнопка должна давать мгновенную визуальную обратную связь.

Изменение должно быть едва заметным.

Пользователь должен ощущать физичность взаимодействия.

Но не видеть чрезмерной анимации.

---

# 63. Keyboard Focus

При работе исключительно с клавиатуры пользователь должен всегда понимать:

какой элемент активен;

какое действие будет выполнено.

Использовать стандартное поведение macOS.

Не изобретать собственную систему фокуса.

---

# 64. Accessibility

Интерфейс обязан корректно работать с:

VoiceOver;

Keyboard Navigation;

Reduce Motion;

Increase Contrast;

Dynamic Type (в пределах возможностей настольного интерфейса);

различными системными настройками доступности.

Доступность рассматривается как обязательное требование, а не как дополнительная функция.

---

# Acceptance Criteria (UI)

Пользовательский интерфейс считается соответствующим требованиям, если:

* при первом запуске создаётся ощущение нативного приложения macOS;
* внешний вид не выглядит как стилизация под Apple, а воспринимается как естественное продолжение системной дизайн-системы;
* интерфейс остаётся аккуратным и читаемым при изменении размеров окна;
* Light Mode и Dark Mode поддерживаются без визуальных артефактов;
* все элементы имеют предсказуемые состояния (обычное, hover, pressed, focus);
* дисплей остаётся читаемым даже при длинных выражениях;
* анимации помогают восприятию интерфейса и не отвлекают пользователя;
* отсутствуют визуальные решения, выбивающиеся из современного языка дизайна macOS.

---

# 65. Пользовательский опыт (User Experience)

## Общая философия UX

Главная задача UX — сделать работу с приложением настолько естественной, чтобы пользователь никогда не задумывался о том, **как** выполнить действие.

Любое действие должно быть очевидным.

Пользователь не должен изучать интерфейс.

Не должен читать инструкцию.

Не должен открывать справку.

Не должен смотреть обучающие видео.

Если приложение требует объяснений, значит UX требует переработки.

---

# 66. Основной пользовательский сценарий

Это самый важный сценарий проекта.

Именно под него оптимизируется вся архитектура приложения.

### Сценарий

Пользователь:

1. открывает приложение;

2. нажимает ⌘V

3. вставляет:

```
(15+16+17+18)/4
```

4. немедленно получает:

```
16.5
```

Никаких дополнительных действий.

Не требуется нажимать "=".

Не требуется выбирать режим.

Не требуется активировать поле ввода.

Не требуется подтверждение.

Весь сценарий должен занимать менее одной секунды.

---

# 67. Вторичный сценарий

Обычное использование мышью.

Пользователь нажимает кнопки.

Получает привычное поведение обычного калькулятора.

Если он никогда не пользуется буфером обмена —

он вообще не замечает, что приложение отличается от Calculator.app.

---

# 68. Работа с клавиатурой

Полноценная клавиатурная поддержка обязательна.

Пользователь должен иметь возможность пользоваться приложением вообще без мыши.

Поддерживаются:

цифры
+
-
*
/
%
(
)
.
Enter
Return
Delete
Backspace
Esc
Tab
Shift+Tab
⌘C
⌘V
⌘A
⌘Z (если действие действительно можно отменить)

Стандартные сочетания клавиш должны вести себя так, как ожидает пользователь macOS.

---

# 69. Поведение клавиши Enter

Нажатие Enter или Return должно:

вычислить выражение;

обновить дисплей;

добавить запись в историю (если вычисление успешно);

при необходимости подготовить приложение к вводу следующего выражения.

Поведение должно быть единообразным.

---

# 70. Поведение клавиши Escape

Esc очищает текущее выражение.

Если выражение уже пустое —

очищает отображаемый результат (при необходимости).

Поведение должно быть документировано и оставаться постоянным.

---

# 71. Поведение Backspace

Удаляется последний введённый символ.

Если удаление делает выражение пустым —

приложение возвращается в исходное состояние без ошибок.

Удаление должно быть мгновенным.

---

# 72. Буфер обмена (UX)

Буфер обмена является основной особенностью приложения.

Он должен ощущаться частью системы.

### Вставка

При вставке математического выражения:

1. выражение автоматически появляется на дисплее;

2. автоматически вычисляется;

3. результат отображается немедленно.

### Вставка числа

Если вставлено одно число —

оно становится текущим выражением.

Никаких дополнительных преобразований.

### Вставка некорректного текста

Если вставленный текст невозможно интерпретировать как математическое выражение —

приложение не должно:

издавать системный звук;

открывать Alert;

блокировать пользователя;

показывать технические ошибки.

Следует использовать небольшое локализованное сообщение внутри интерфейса.

---

# 73. Копирование

Команда Copy должна работать максимально естественно.

Если пользователь скопировал результат —

в буфер попадает только результат.

Например:

```
16.5
```

Если в будущем будет реализовано выделение выражения —

копироваться должно именно выделенное содержимое.

---

# 74. Автоматическое копирование результата

Опциональная настройка.

Если пользователь включил её —

после успешного вычисления результат автоматически помещается в буфер обмена.

По умолчанию функция отключена.

Она не должна менять основной сценарий работы.

---

# 75. История вычислений

История должна быть максимально простой.

Она не является основной функцией приложения.

История существует только для удобства.

### История содержит:

выражение;

результат;

дату и время (необязательно отображать по умолчанию).

### Размер истории:

По умолчанию рекомендуется хранить последние 50 вычислений.

Размер должен быть легко изменяемым в коде.

### Просмотр истории:

История открывается ненавязчиво.

Не отдельным окном.

Не полноэкранным режимом.

Предпочтительно:

popover;

sheet;

выдвижная боковая панель (если она не нарушает минимализм).

Главное —

не усложнить основной сценарий.

### Повторное использование:

Пользователь может нажать на запись истории.

Выражение возвращается на дисплей.

При необходимости сразу вычисляется повторно.

---

# 76. Настройки

Настройки должны быть минимальными.

Их количество должно стремиться к нулю.

Рекомендуемые настройки:

автоматически копировать результат;

показывать разделители тысяч;

максимальное количество знаков после запятой (при необходимости);

размер истории;

очистить историю.

Добавление новых настроек требует серьёзного обоснования.

---

# 77. Первое открытие приложения

Первый запуск должен быть полностью готов к работе.

Не показывать:

обучение;

мастер настройки;

экран приветствия;

советы;

маркетинговые окна.

Пользователь сразу видит калькулятор.

И сразу может работать.

---

# 78. Поведение после запуска

После открытия приложения:

фокус ввода должен быть готов к работе;

можно сразу печатать;

можно сразу вставлять выражение;

можно сразу пользоваться клавиатурой.

Никаких дополнительных кликов.

---

# 79. Сохранение состояния

После закрытия приложения желательно сохранить:

размер окна;

позицию окна;

выбранную тему (если отличается от системной);

настройки;

историю.

Текущее незавершённое выражение можно восстанавливать только в том случае, если это не приводит к неожиданному поведению.

---

# 80. Микровзаимодействия

Микровзаимодействия должны быть едва заметными.

Например:

небольшое изменение материала кнопки;

лёгкое изменение прозрачности;

короткая анимация обновления результата;

мягкое появление сообщения об ошибке.

Запрещаются:

прыгающие элементы;

резкие вспышки;

сложные переходы;

декоративные эффекты.

---

# 81. Сообщения об ошибках (UX)

Ошибки должны восприниматься спокойно.

Они не должны создавать ощущение, что приложение "сломалось".

Сообщение должно:

быть коротким;

понятным;

локализуемым;

не обвинять пользователя.

Например:

"Неверное выражение"

лучше, чем

"Ошибка синтаксического анализа."

---

# 82. Локализация

Архитектура приложения должна изначально учитывать возможность локализации.

Все пользовательские строки должны быть вынесены в локализуемые ресурсы.

Не допускается наличие "захардкоженного" текста в интерфейсе.

Минимальный набор языков:

English

Русский

Архитектура должна позволять безболезненно добавлять новые языки.

---

# 83. Accessibility (UX)

Приложение должно корректно работать с системными средствами доступности.

Необходимо поддерживать:

VoiceOver;

полноценную клавиатурную навигацию;

системные настройки контрастности;

Reduce Motion;

увеличенный размер интерфейсных элементов там, где это применимо.

Каждый интерактивный элемент должен иметь понятное accessibilityLabel и, при необходимости, accessibilityHint.

---

# 84. Производительность пользовательского интерфейса

Интерфейс должен оставаться отзывчивым при любых допустимых действиях пользователя.

Любые потенциально длительные операции не должны блокировать главный поток.

При обновлении интерфейса следует минимизировать количество перерисовок и вычислений.

---

# Acceptance Criteria (UX)

UX считается соответствующим требованиям, если:

* пользователь способен начать работу сразу после запуска приложения;
* основной сценарий "⌘V → результат" выполняется без дополнительных действий;
* приложение полностью управляется с клавиатуры;
* история не отвлекает от основной задачи, но доступна при необходимости;
* сообщения об ошибках понятны и не нарушают рабочий процесс;
* настройки остаются минимальными и не перегружают интерфейс;
* приложение ведёт себя предсказуемо и соответствует ожиданиям пользователя macOS.

---

# 86. Производительность

## Общие требования

Производительность рассматривается как одна из ключевых характеристик качества приложения.

Пользователь никогда не должен ждать выполнения обычной операции.

Любое действие должно восприниматься мгновенным.

---

## Время запуска

Приложение должно запускаться максимально быстро.

После запуска пользователь должен сразу иметь возможность:

* вводить выражение;
* вставлять выражение;
* пользоваться клавиатурой.

Не допускается блокировка интерфейса во время инициализации.

---

## Отзывчивость интерфейса

Все операции пользовательского интерфейса должны выполняться на главном потоке только тогда, когда это действительно необходимо.

Вычисления, не требующие обновления интерфейса, не должны приводить к заметным задержкам или "подвисаниям".

---

## Использование памяти

Приложение должно использовать разумный объём памяти.

Не допускается:

* бесконтрольный рост памяти;
* утечки памяти;
* retain cycle;
* хранение ненужных объектов;
* бесконечный рост истории вычислений.

---

# 87. Swift Concurrency

Проект должен использовать современную модель конкурентности Swift.

Использовать:

* async/await;
* actor там, где это действительно улучшает потокобезопасность;
* Sendable для подходящих типов;
* @MainActor только для компонентов, непосредственно связанных с пользовательским интерфейсом.

Не использовать GCD напрямую без объективной необходимости.

Избегать смешивания нескольких моделей конкурентности.

---

# 88. Безопасность

Несмотря на локальный характер приложения, необходимо соблюдать хорошие инженерные практики.

Приложение должно быть устойчиво к:

* некорректному пользовательскому вводу;
* очень длинным выражениям;
* неожиданным символам;
* пустому буферу обмена;
* повреждённым данным истории;
* неожиданному завершению работы.

Ни один из этих сценариев не должен приводить к аварийному завершению приложения.

---

# 89. Логирование

В Release-сборке логирование должно быть минимальным.

Не выводить:

* внутренние структуры данных;
* содержимое пользовательских выражений без необходимости;
* диагностические сообщения разработки.

Для отладочных сборок допускается расширенное логирование.

---

# 90. Тестирование

## Общие требования

Каждый модуль должен иметь автоматические тесты.

Минимальные категории тестов:

* Unit Tests;
* UI Tests.

---

## Unit Tests

Необходимо протестировать:

Tokenizer

Parser

Evaluator

Clipboard Parser

Formatting

History

Utilities

Особое внимание уделить пограничным случаям.

Например:

пустая строка;

одно число;

очень длинное выражение;

глубоко вложенные скобки;

деление на ноль;

несколько операторов подряд;

некорректный ввод;

отрицательные числа;

проценты.

---

## UI Tests

Проверить:

открытие приложения;

ввод с клавиатуры;

нажатия кнопок;

вставку выражения;

копирование результата;

отображение ошибок;

историю;

работу Light Mode;

работу Dark Mode.

---

## Regression Tests

Каждая найденная ошибка должна сопровождаться тестом, предотвращающим её повторное появление.

---

# 91. Тестирование специальных случаев

## Обязательные тесты:

Для каждого специального случая из секции 39 необходимо написать минимум один Unit Test.

## Список тестов:

### Тесты для Tokenizer:

```swift
class TokenizerTests: XCTestCase {
    func testTokenize_EmptyString() {
        let tokenizer = Tokenizer()
        let tokens = tokenizer.tokenize("")
        XCTAssertTrue(tokens.isEmpty)
    }
    
    func testTokenize_WithEqualsSign() {
        let tokenizer = Tokenizer()
        let tokens = tokenizer.tokenize("15+16=")
        // "=" должен быть проигнорирован
        XCTAssertEqual(tokens.count, 3) // 15, +, 16
    }
    
    func testTokenize_MultipleLines() {
        let tokenizer = Tokenizer()
        let tokens = tokenizer.tokenize("15+16\n20+30")
        // Должна обработаться только первая строка
        XCTAssertEqual(tokens.count, 3)
    }
    
    func testTokenize_ExponentialNotation() {
        let tokenizer = Tokenizer()
        let tokens = tokenizer.tokenize("1.5e3")
        // Должен быть один токен Number(1500)
        XCTAssertEqual(tokens.count, 1)
    }
    
    func testTokenize_HexNumber() {
        let tokenizer = Tokenizer()
        let tokens = tokenizer.tokenize("0xFF")
        // Должен быть один токен Number(255)
        XCTAssertEqual(tokens.count, 1)
    }
    
    func testTokenize_Constants() {
        let tokenizer = Tokenizer()
        let tokens = tokenizer.tokenize("π")
        // Должен быть один токен Number(3.14159...)
        XCTAssertEqual(tokens.count, 1)
    }
}
```

### Тесты для Parser:

```swift
class ParserTests: XCTestCase {
    func testParse_InvalidCharacter() {
        let parser = Parser()
        let tokens: [Token] = [.number(15), .operator(.add), .invalidCharacter("a")]
        XCTAssertThrowsError(try parser.parse(tokens))
    }
    
    func testParse_MissingParenthesis() {
        let parser = Parser()
        let tokens: [Token] = [.leftParenthesis, .number(15), .operator(.add), .number(16)]
        XCTAssertThrowsError(try parser.parse(tokens))
    }
    
    func testParse_ExtraParenthesis() {
        let parser = Parser()
        let tokens: [Token] = [.number(15), .operator(.add), .number(16), .rightParenthesis]
        XCTAssertThrowsError(try parser.parse(tokens))
    }
}
```

### Тесты для Evaluator:

```swift
class EvaluatorTests: XCTestCase {
    func testEvaluate_DivisionByZero() {
        let evaluator = Evaluator()
        // Создать AST для "1/0"
        XCTAssertThrowsError(try evaluator.evaluate(ast))
    }
    
    func testEvaluate_Percent() {
        let evaluator = Evaluator()
        // Тест: 50% = 0.5
        let result = try evaluator.evaluate(ast)
        XCTAssertEqual(result, 0.5)
    }
    
    func testEvaluate_DoubleOperators() {
        let evaluator = Evaluator()
        // Тест: 15++16 должен вызвать ошибку
        XCTAssertThrowsError(try evaluator.evaluate(ast))
    }
}
```

### Тесты для CalculatorEngine (интеграционные):

```swift
class CalculatorEngineTests: XCTestCase {
    func testEvaluate_AllSpecialCases() {
        let cases: [(String, Decimal?)] = [
            ("15+16", 31),
            ("(15+16)*5", 155),
            ("-5+12", 7),
            ("-(15+16)", -31),
            ("3.1415*5", 15.7075),
            ("50%", 0.5),
            ("100 * 5%", 5),
            ("100 / 5%", 2000),
            ("0xFF", 255),
            ("0b1010", 10),
            ("0o77", 63),
            ("1.5e3", 1500),
            ("π", 3.14159265358979),
            ("e", 2.71828182845905),
            ("π * 2", 6.28318530717958),
        ]
        
        for (expression, expected) in cases {
            let result = try? CalculatorEngine.evaluate(expression)
            XCTAssertEqual(result, expected, "Failed for: \(expression)")
        }
    }
    
    func testEvaluate_AllErrorCases() {
        let cases = [
            "1/0",
            "15+a",
            "(15+16",
            "15+16)",
            "NaN",
            "Infinity",
        ]
        
        for expression in cases {
            let result = try? CalculatorEngine.evaluate(expression)
            XCTAssertNil(result, "Should fail for: \(expression)")
        }
    }
}
```

## Покрытие тестами:

- Минимум 80% кода должно быть покрыто тестами
- Все специальные случаи из секции 39 должны иметь тесты
- Все ошибки должны иметь тесты

## Автоматизация:

Тесты должны запускаться автоматически при каждом коммите (CI/CD).

---

# 92. Code Quality

Код должен быть написан так, словно его будет поддерживать другая команда разработчиков.

Следовать следующим принципам:

* SOLID (там, где это оправдано);
* KISS;
* DRY;
* YAGNI.

Не применять шаблоны проектирования без реальной необходимости.

---

## Именование

Все имена должны быть понятными.

Не использовать сокращения без необходимости.

Предпочитать полные, читаемые названия.

---

## Размер функций

Функции должны быть небольшими.

Каждая функция должна выполнять одну логически завершённую задачу.

---

## Размер типов

Избегать чрезмерно больших View, ViewModel и сервисов.

Если тип становится слишком большим, необходимо пересмотреть его ответственность.

---

## Документация

Документировать:

* публичные API;
* сложные алгоритмы;
* архитектурные решения.

Не документировать очевидный код.

Комментарии должны объяснять **почему**, а не **что** делает код.

---

# 93. Definition of Done

Работа считается завершённой только при выполнении всех условий ниже.

## Архитектура

* Все модули имеют одну ответственность.
* Отсутствуют циклические зависимости.
* Архитектура соответствует спецификации.

---

## Код

* Нет TODO.
* Нет временных решений.
* Нет закомментированного кода.
* Нет неиспользуемых файлов.
* Нет предупреждений компилятора.

---

## Функциональность

Работают:

* вычисления;
* скобки;
* проценты;
* отрицательные числа;
* буфер обмена;
* история;
* горячие клавиши.

---

## Интерфейс

Интерфейс:

* соответствует философии проекта;
* выглядит нативно;
* корректно работает в Light и Dark Mode;
* масштабируется без визуальных дефектов.

---

## Производительность

Нет заметных задержек.

Нет утечек памяти.

Нет блокировки главного потока.

---

## Тесты

Все тесты успешно проходят.

---

# 94. Финальный аудит

Перед завершением проекта необходимо провести самостоятельный аудит.

Проверить:

Архитектуру.

Качество кода.

UX.

UI.

Производительность.

Accessibility.

Локализацию.

Буфер обмена.

Парсер.

Историю.

Настройки.

Горячие клавиши.

Обработку ошибок.

---

После проверки необходимо выполнить дополнительный рефакторинг.

Удалить:

* дублирование;
* ненужные зависимости;
* лишние абстракции;
* неиспользуемый код.

Повторять аудит до тех пор, пока дальнейшие улучшения перестанут давать заметный эффект.

---

# 95. Release Checklist

Перед тем как считать приложение готовым к публикации, необходимо убедиться, что:

□ Проект полностью собирается.

□ Все тесты проходят.

□ Нет предупреждений компилятора.

□ Нет утечек памяти.

□ Нет retain cycle.

□ Нет блокировки Main Thread.

□ Работает ⌘V.

□ Работает ⌘C.

□ Работает клавиатурный ввод.

□ Работают кнопки мышью.

□ Корректно отображаются ошибки.

□ Поддерживаются вложенные скобки.

□ Поддерживаются длинные выражения.

□ Поддерживаются отрицательные числа.

□ Работает история.

□ Работают настройки.

□ Работает локализация.

□ Работает Accessibility.

□ Работают Light Mode и Dark Mode.

□ Интерфейс выглядит нативным.

□ Не используется неподдерживаемый API.

□ Отсутствуют лишние зависимости.

□ Приложение работает полностью локально.

---

# 96. Итоговые Acceptance Criteria

Проект считается полностью успешным только в том случае, если выполняются все следующие условия:

1. Пользователь открывает приложение и сразу понимает, как им пользоваться.

2. Пользователь может вставить математическое выражение через ⌘V и мгновенно получить корректный результат.

3. Пользователь может полностью управлять приложением с клавиатуры.

4. Интерфейс выглядит и ощущается как современное нативное приложение macOS.

5. Приложение не содержит лишних функций и остаётся сфокусированным на своей единственной задаче.

6. Архитектура проекта проста, понятна и пригодна для долгосрочной поддержки.

7. Код соответствует современным рекомендациям Apple, Swift API Design Guidelines и принципам качественной инженерной разработки.

8. Приложение работает полностью локально, не требует подключения к сети и уважает конфиденциальность пользователя.

9. После нескольких дней использования пользователь воспринимает приложение не как сторонний инструмент, а как естественную замену системному Calculator.app.

10. Главная цель проекта достигнута только в том случае, если пользователь искренне задаётся вопросом:

> «Почему Apple сама до сих пор не сделала именно так?»

---

# Заключение

Настоящая спецификация определяет не только функциональность приложения, но и философию его разработки.

При возникновении любого спорного вопроса приоритет требований должен быть следующим:

1. Простота для пользователя.
2. Соответствие современному UX macOS.
3. Соответствие Apple Human Interface Guidelines.
4. Производительность.
5. Поддерживаемость кода.
6. Расширяемость архитектуры.

Любое решение, противоречащее этим принципам, должно быть пересмотрено до начала реализации.

Конечной целью является создание приложения, которое ощущается как недостающее звено в экосистеме macOS: максимально нативного, минималистичного, производительного и удобного калькулятора с полноценной поддержкой математических выражений и буфера обмена.

---

## Приложение А: Проверка среды разработки

### Дата проверки: 2026-07-04 16:30
### Проверено агентом: OrnithQ8

### Результаты проверки системы:

```bash
# Проверка версии macOS
$ sw_vers
ProductName:		macOS
ProductVersion:		26.5.1
BuildVersion:		25F80

# Проверка установки Xcode Command Line Tools
$ xcode-select -p
/Library/Developer/CommandLineTools

# Проверка версии Swift
$ swift --version
swift-driver version: 1.148.6 Apple Swift version 6.3.3 (swiftlang-6.3.3.1.3 clang-2100.1.1.101)
Target: arm64-apple-macosx26.0

# Проверка версии Git
$ git --version
git version 2.55.0
```

### Выводы:

✅ **macOS 26.5.1** - установлена и готова к разработке  
✅ **Swift 6.3.3** - установлен (совместим с планом, требуется Swift 6+)  
✅ **Xcode Command Line Tools** - установлены  
✅ **Git 2.55.0** - установлен  

### Статус готовности:

**ВСЕ НЕОБХОДИМЫЙ ИНСТРУМЕНТЫ УСТАНОВЛЕНЫ.**

Можно сразу приступать к разработке проекта калькулятора для macOS согласно настоящему плану.

### Примечания:

1. Установлены Xcode Command Line Tools, а не полный Xcode. Для SwiftUI-разработки рекомендуется установить полный Xcode из App Store (около 12 ГБ) для доступа к SwiftUI Preview и другим инструментам визуальной разработки.

2. Однако для компиляции и запуска приложения достаточно текущей конфигурации.

3. Архитектура процессора: arm64 (Apple Silicon) - совместима с целевой платформой.

---

## Инструкция для продолжения работы после перезагрузки:

1. Прочитать данный документ полностью
2. Выполнить проверку среды разработки (команды из Приложения А)
3. Если все инструменты установлены - приступить к реализации проекта
4. Следовать плану пошагово, начиная с Части 1 (исправления недостатков), затем Часть 2 (реализация)