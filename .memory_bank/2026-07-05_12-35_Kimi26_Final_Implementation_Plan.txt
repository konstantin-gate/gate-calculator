{
  "content": "---\n# Финальный план реализации GateCalc\n# Устранение всех недостатков на основе 4 аудитов\n\n**Дата:** 2026-07-05\n**Время:** 13:10\n**Агент:** Kimi26\n**Статус:** Готов к пошаговому исполнению\n\n---\n\n## Введение\n\nНастоящий документ единый, исчерпывающий и готовый к исполнению план устранения всех проблем.\nВсе утверждения верифицированы путём прямого чтения исходного кода проекта GateCalc.\n\n---\n\n## Общая архитектура плана\n\nПлан разделён на 5 фаз:\n1. Критические исправления (блокируют DoD)\n2. Исправление ядра (Tokenizer / Parser / Evaluator)\n3. UI/UX и архитектурные исправления\n4. Локализация и доступность\n5. Тестирование и финализация\n\nВнутри каждого шага: файл, строки, old → new, подтверждение.\n\n---\n\n## Фаза 1. Критические исправления (блокируют DoD)\n\n### Шаг 1.1. Удалить научный режим (Scientific Mode)\n\n**Причина:** Нарушает SRS §11 (категорический запрет). Научные функции не реализованы в движке, кнопки не работают.\n\n**Файлы:**\n- Sources/Views/CalculatorView.swift\n- Sources/ViewModels/CalculatorViewModel.swift\n\n**Что сделать:**\n1. Удалить `enum CalculatorMode` (строки 5-8).\n2. Удалить `@State private var mode: CalculatorMode = .standard`.\n3. Удалить свойства `buttonDiameter`, `buttonFontSize`, `buttonSpacing` (оставить константы, не зависящие от режима).\n4. Удалить `scientificButtonGrid` (весь блок).\n5. Удалить все кейсы научных кнопок в `handleButtonPress` и `.secondFunc` с `break // TODO`.\n6. В `CalculatorViewModel.swift` удалить: `isRadians`, `toggleRadDeg()`, `appendFunction`, `insertRandom`.\n\n**Подтвердить:** Приложение компилируется и запускается только в стандартном режиме.\n\n---\n\n### Шаг 1.2. Зарегистрировать тесты в Package.swift\n\n**Причина:** Без `.testTarget` команда `swift test` выполнит 0 тестов. Блокирует DoD.\n\n**Файл:** Package.swift\n\n**Что сделать (послад 48):** Добавить новый target:\n\n```swift\n        .testTarget(\n            name: \"CalculatorTests\",\n            dependencies: [\"CalculatorEngine\"],\n            path: \"Tests\"\n        ),\n```\n\n**Подтвердить:** Выполнить `swift test` и убедиться, что все тесты проходят.\n\n---\n\n### Шаг 1.3. Исправить `char.lowercased() == \"pi\"` в Tokenizer\n\n**Причина:** `Character.lowercased()` возвращает `String` одного символа. Сравнение `\"p\" == \"pi\"` всегда `false`. Токен «pi» не распознаётся.\n\n**Файл:** Sources/CalculatorEngine/Tokenizer/Tokenizer.swift\n\n**Что сделать (строка 90):** Заменить:\n```swift\nif char == \"\\u{03C0}\" || char == \"\\u{03C0}\" || char.lowercased() == \"pi\" {\n```\nна:\n```swift\nif char == \"\\u{03C0}\" || char == \"\u{03C0}\" {\n```\n\nДобавить отдельную логику для многосимвольного «pi»:\n\n```swift\n            if char == \"p\" || char == \"P\" {\n                let nextIndex = cleaned.index(after: i)\n                if nextIndex < cleaned.endIndex {\n                    let nextChar = cleaned[nextIndex]\n                    if nextChar == \"i\" || nextChar == \"I\" {\n                        tokens.append(.number(Decimal.pi))\n                        advance(&amp;i, in: cleaned)\n                        advance(&amp;i, in: cleaned)\n                        continue\n                    }\n                }\n            }\n\n            if char == \"\u{03C0}\" || char == \"\u{03C0}\" {\n                tokens.append(.number(Decimal.pi))\n                advance(&amp;i, in: cleaned)\n                continue\n            }\n```\n\n**Подтвердить:** `testTokenize_PiKeyword` проходит.\n\n---\n\n### Шаг 1.4. Исправить тип ошибки в Parser (extra вместо missing)\n\n**Причина:** При незакрытой скобке `(15+16` Parser выбрасывает `extraClosingParenthesis` вместо `missingClosingParenthesis`.\n\n**Файл:** Sources/CalculatorEngine/Parser/Parser.swift\n\n**Что сделать (строка 86):** Заменить:\n```swift\n                throw CalculatorError.extraClosingParenthesis\n```\nна:\n```swift\n                throw CalculatorError.missingClosingParenthesis\n```\n\n**Подтвердить:** `testParse_MissingClosingParenthesis` ожидает `missingClosingParenthesis`.\n\n---\n\n### Шаг 1.5. Исправить вставку из буфера (expression очищается)\n\n**Причина:** При вставке через ⌘V выражение очищается (`expression = \"\"`). Пользователь не видит вставленное выражение.\n\n**Файл:** Sources/ViewModels/CalculatorViewModel.swift\n\n**Что сделать (строки 182-188):** Заменить:\n```swift\n        if let value = try? engine.evaluate(trimmed) {\n            expression = \"\"\n            result = formatter.format(value)\n            historyService.add(expression: trimmed, result: value)\n        } else {\n            errorMessage = \"Cannot evaluate\"\n        }\n```\nна:\n```swift\n        if let value = try? engine.evaluate(trimmed) {\n            expression = trimmed\n            result = formatter.format(value)\n            historyService.add(expression: trimmed, result: value)\n        } else {\n            expression = trimmed\n            errorMessage = \"Cannot evaluate\"\n        }\n```\n\n**Подтвердить:** После вставки выражение отображается на дисплее.\n\n---\n\n### Шаг 1.6. Исправить экспоненциальное форматирование (\"e+\" → \"E+\")\n\n**Причина:** `NumberFormatter` с `.scientific` выдаёт «E+», а код ищет «e+».\n\n**Файл:** Sources/Formatting/NumberFormatterService.swift\n\n**Что сделать (строка 29):** Заменить:\n```swift\n                return formatted.replacingOccurrences(of: [\"e+\"], with: [\"\"])\n```\nна:\n```swift\n                return formatted.replacingOccurrences(of: [\"E+\"], with: [\"\"])\n```\n\n**Подтвердить:** Тесты с экспоненциальными числами проходят.\n\n---\n\n### Шаг 1.7. Исправить скрытый баг: formatter записывает локализованную строку в expression\n\n**Причина:** `formatter.format(dec)` для `ru_RU` возвращает «1,5». Tokenizer не ожидает запятую → бесконечный цикл.\n\n**Файл:** Sources/ViewModels/CalculatorViewModel.swift\n\n**Что сделать:** В `appendCharacter` (строки 31-32) и `appendOperator` (строки 45-46) заменить `formatter.format(dec)` на `dec.description`.\n\n**Подтвердить:** При русской локали после вычисления `1.5 + 2` и нажатия `+`, в expression записывается `3.5` (с точкой).\n\n---\n\n### Шаг 1.8. Добавить `Components/` и `Extensions/` в sources Package.swift\n\n**Причина:** Пропущены папки `Components/`, `Extensions/`. Файлы в них не компилируются.\n\n**Файл:** Package.swift\n\n**Что сделать (строки 32-42):** Добавить `Components` и `Extensions` в список sources.\n\n**Подтвердить:** `swift build` проходит без ошибок.\n\n---\n\n## Фаза 2. Исправление ядра (Tokenizer / Parser)\n\n### Шаг 2.1. Исправить потенциальные бесконечные циклы в Tokenizer\n\n**Причина:** Для входных данных `\".\"`, `\"1..5\"`, `\"0x\"`, `\"0b\"`, `\"0o\"` функция `readNumber` возвращает `(0, start)` без продвижения индекса → бесконечный цикл.\n\n**Файл:** Sources/CalculatorEngine/Tokenizer/Tokenizer.swift\n\n**Что сделать:**\n1. Для точки (`\".\"`): В `readNumber`, если `numberStr == \".\"`, бросить `CalculatorError.invalidExpression`.\n2. Для двойной точки: После первой точки, если встречается вторая — бросить `CalculatorError.invalidExpression`.\n3. Для `0x`, `0b`, `0o` без цифр: В `readHex`, `readBinary`, `readOctal` — если `hexStr.isEmpty`, `binStr.isEmpty`, `octStr.isEmpty`, бросить `CalculatorError.invalidExpression`.\n\n**Подтвердить:** Тесты для `\".\"`, `\"1..5\"`, `\"0x\"`, `\"0b\"`, `\"0o\"` проходят (ожидается ошибка).\n\n---\n\n### Шаг 2.2. Исправить приоритет `unaryMinus` vs `percent`\n\n**Причина:** SRS §28 требует, чтобы унарный минус и %, /, * были на одном уровне.\n\n**Файл:** Sources/CalculatorEngine/Parser/Precedence.swift\n\n**Что сделать (строки 3-8):** Заменить:\n```swift\nenum Precedence: Int, Sendable {\n    case addition = 1\n    case multiplication = 2\n    case unaryMinus = 3\n    case percent = 4\n}\n```\nна:\n```swift\nenum Precedence: Int, Sendable {\n    case addition = 1\n    case multiplication = 2\n    case unaryMinus = 2\n    case percent = 2\n}\n```\n\n**Подтвердить:** `-5%` даёт `-0.05` (совпадает), но семантика верна: уровни приоритета совпадают.\n\n---\n\n### Шаг 2.3. Исправить форматирование тысячных разделителей\n\n**Причина:** Для чисел с тысячными разделителями (из форматера) `Decimal(string:)` возвращает `nil`.\n\n**Файл:** Sources/ViewModels/CalculatorViewModel.swift\n\n**Что сделать:** В `toggleSign()`, `memoryAdd()`, `memorySubtract()` заменить `Decimal(string:)` на `Decimal(string:replacingOccurrences(of: \",\", with: \"\"))`.\n\n**Подтвердить:** `toggleSign()` работает с числами типа `\"1,234.56\"`.\n\n---\n\n## Фаза 3. UI/UX и архитектурные исправления\n\n### Шаг 3.1. Исправить размеры окна до 350×520\n\n**Файл:** Sources/App/CalculatorApp.swift\n\n**Что сделать (строка 14):** Заменить `.defaultSize(width: 340, height: 490)` на `.defaultSize(width: 350, height: 520)`.\n\n---\n\n### Шаг 3.2. Удалить `.design(.rounded)` везде, заменить на SF Pro\n\n**Файлы:** Sources/Views/DisplayView.swift, Sources/Views/CalculatorButton.swift.\n\n**Что сделать:** Заменить `.system(..., design: .rounded)` на `.system(..., design: .default)`.\n\n---\n\n### Шаг 3.3. Привести размеры шрифтов к спецификации SRS\n\n**Файл:** Sources/Views/DisplayView.swift\n\n**Что сделать:**\n- Строка 17 (`expression`): `size: 18` → `size: 20`.\n- Строка 31 (`error`): `size: 22` → `size: 24`.\n- Строки 94-98 (`adaptiveFont`): 70→42, 56→36, 44→30, 32→24.\n\n---\n\n### Шаг 3.4. Удалить hardcoded RGB цвета, заменить на системные\n\n**Файл:** Sources/Theme/CalculatorColors.swift\n\n**Что сделать (строки 18-32):** Заменить `buttonDigit` и `buttonFunction` на системные `Color(...)`.\n\n---\n\n### Шаг 3.5. Исправить размеры кнопок до 60pt\n\n**Файл:** Sources/Views/CalculatorView.swift\n\n**Что сделать (строки 19-21):** buttonDiameter=60, buttonFontSize=22, buttonSpacing=8.\n\n---\n\n### Шаг 3.6. Удалить TODO из production-кода\n\n**Файл:** Sources/Views/CalculatorView.swift\n\n**Что сделать (строка 241):** Удалить `break // TODO`.\n\n---\n\n### Шаг 3.7. Удалить мёртвый код: Decimal+Extensions, EmptyStateView, isMathExpression\n\n**Файлы:**\n- Sources/Extensions/Decimal+Extensions.swift — удалить.\n- Sources/Components/EmptyStateView.swift — удалить.\n- Sources/Extensions/String+Calculator.swift — удалить.\n\n**Подтвердить:** `grep` показывает 0 упоминаний.\n\n---\n\n### Шаг 3.8. Исправить HistoryPanelView: убрать прямое обращение к singleton\n\n**Файл:** Sources/Views/HistoryPanelView.swift\n\n**Что сделать:** Заменить `HistoryService.shared.getEntries()` и `HistoryService.shared.clear()` на обращение через `viewModel`. Добавить `historyEntries` и `clearHistory()` в `ViewModel`.\n\n---\n\n### Шаг 3.9. Добавить `@MainActor` к KeyHandlerNSView\n\n**Файл:** Sources/App/CalculatorApp.swift\n\n**Что сделать (строка 50):** Заменить `class KeyHandlerNSView: NSView {` на `@MainActor class KeyHandlerNSView: NSView {`.\n\n---\n\n## Фаза 4. Локализация и доступность\n\n### Шаг 4.1. Заменить жестко закодированные строки ошибок на NSLocalizedString\n\n**Файл:** Sources/CalculatorEngine/Errors/CalculatorError.swift\n\n**Что сделать (строки 42-61):** Заменить все строки в `errorDescription` на `NSLocalizedString(..., comment: ...)`.\n\n**Подтвердить:** Тесты ожидают локализованные строки.\n\n---\n\n### Шаг 4.2. Добавить локализацию HistoryPanelView\n\n**Файл:** Sources/Views/HistoryPanelView.swift\n\n**Что сделать:** Заменить «No History», «Calculations will appear here», «History», «Done», «Clear» на `NSLocalizedString(...)`.\n\n---\n\n### Шаг 4.3. Учёт локали для formattedExpression\n\n**Файл:** Sources/Views/DisplayView.swift\n\n**Что сделать (строка 108):** Убрать безусловную замену `"." → ","`. Заменять только если текущая локаль использует запятую.\n\n---\n\n### Шаг 4.4. Добавить Reduce Motion для DisplayView\n\n**Файл:** Sources/Views/DisplayView.swift\n\n**Что сделать:** Добавить `@Environment(\\.accessibilityReduceMotion) private var reduceMotion` и условно применять анимации.\n\_PKEY\n\n## Фаза 5. Тестирование и финализация\n\n### Шаг 5.1. Исправить сравнение `Decimal` через `.description` в тестах\n\n**Файлы:** Tests/Unit/CalculatorEngineTests.swift, Tests/Unit/EvaluatorTests.swift.\n\n**Что сделать:** Заменить `result.description` на `result`, `expected.description` на `expected`.\n\n---\n\n### Шаг 5.2. Обновить `testTokenize_PiKeyword`\n\n**Файл:** Tests/Unit/TokenizerTests.swift.\n\n**Что сделать (строки 125-129):** Убедиться, что тест ожидает `Decimal.pi`.\n\n---\n\n### Шаг 5.3. Обновить тест `testTokenize_EulerNumberWithExponent`\n\n**Файл:** Tests/Unit/TokenizerTests.swift.\n\n**Что сделать (строки 142-151):** Тест неверен. Либо исправить тест (ожидать ошибку для `e3`), либо удалить.\n\n---\n\n## Контрольный список для агента\n\n| # | Шаг | Файл(-ы) | Функция-тест | Статус |\n|---|-----|---------|-------------|--------|\n| 1.1 | Удалить научный режим | CalculatorView, ViewModel | — | Ожидается |\n| 1.2 | Зарегистрировать тесты | Package.swift | swift test | Ожидается |\n| 1.3 | Исправить "pi" | Tokenizer.swift | testTokenize_PiKeyword | Ожидается |\n| 1.4 | Исправить ошибку скобок | Parser.swift | testParse_MissingClosingParenthesis | Ожидается |\n| 1.5 | Исправить вставку из буфера | CalculatorViewModel.swift | ⌘V в UI | Ожидается |\n| 1.6 | Исправить экспоненциальный формат | NumberFormatterService.swift | Экспоненциальные числа | Ожидается |\n| 1.7 | Исправить локаль в expression | CalculatorViewModel.swift | Вычисление после результата | Ожидается |\n| 1.8 | Добавить sources | Package.swift | swift build | Ожидается |\n| 2.1 | Исправить бесконечные циклы | Tokenizer.swift | Граничные тесты | Ожидается |\n| 2.2 | Исправить приоритеты | Precedence.swift | testParse_Percent | Ожидается |\n| 2.3 | Исправить числа с разделителями | CalculatorViewModel.swift | toggleSign | Ожидается |\n| 3.1 | Исправить размеры окна | CalculatorApp.swift | Запуск приложения | Ожидается |\n| 3.2 | Удалить .rounded | DisplayView.swift, CalculatorButton.swift | Визуальная проверка | Ожидается |\n| 3.3 | Привести шрифты | DisplayView.swift | Визуальная проверка | Ожидается |\n| 3.4 | Удалить hardcoded RGB | CalculatorColors.swift | Визуальная проверка | Ожидается |\n| 3.5 | Исправить размеры кнопок | CalculatorView.swift | Визуальная проверка | Ожидается |\n| 3.6 | Удалить TODO | CalculatorView.swift | Отсутствие TODO в коде | Ожидается |\n| 3.7 | Удалить мёртвый код | Extensions/, Components/ | Проверка grep | Ожидается |\n| 3.8 | Исправить HistoryPanelView | HistoryPanelView.swift, ViewModel | Открытие истории | Ожидается |\n| 3.9 | Добавить @MainActor | CalculatorApp.swift | swift build | Ожидается |\n| 4.1 | Локализация ошибок | CalculatorError.swift | swift test | Ожидается |\n| 4.2 | Локализация HistoryPanelView | HistoryPanelView.swift | Проверка NSLocalizedString | Ожидается |\n| 4.3 | Учёт локали для formattedExpression | DisplayView.swift | Визуальная проверка | Ожидается |\n| 4.4 | Reduce Motion | DisplayView.swift | Проверка accessibilityReduceMotion | Ожидается |\n| 5.1 | Исправить .description в тестах | *Tests.swift | swift test | Ожидается |\n| 6.1 | Обновить testTokenize_PiKeyword | TokenizerTests.swift | swift test | Ожидается |\n| 6.2 | Обновить testTokenize_EulerNumberWithExponent | TokenizerTests.swift | swift test | Ожидается |\n\n---\n\n## Заключение\n\nНастоящий план покрывает все проблемы из четырёх аудитов. План однозначен и готов к пошаговому исполнению.\nПеред началом исполнения рекомендуется сохранить копию проекта и выполнить `swift build`."